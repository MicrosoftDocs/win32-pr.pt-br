---
title: Identificadores de contexto estrito e de tipo estrito
description: Use o \_ atributo de identificador de contexto estrito \_ para todos os identificadores de contexto.
ms.assetid: 2483aa76-0814-4f63-9c38-0aa050d73772
ms.topic: article
ms.date: 05/31/2018
ms.openlocfilehash: a45b4e7914034e315f2275e1d928293332012fc4
ms.sourcegitcommit: 592c9bbd22ba69802dc353bcb5eb30699f9e9403
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 08/20/2020
ms.locfileid: "104454209"
---
# <a name="strict-and-type-strict-context-handles"></a><span data-ttu-id="c6c31-103">Identificadores de contexto estrito e de tipo estrito</span><span class="sxs-lookup"><span data-stu-id="c6c31-103">Strict and Type Strict Context Handles</span></span>

<span data-ttu-id="c6c31-104">Use o atributo de [**\_ \_ identificador de contexto estrito**](/windows/desktop/Midl/strict-context-handle) para todos os identificadores de contexto.</span><span class="sxs-lookup"><span data-stu-id="c6c31-104">Use the [**strict\_context\_handle**](/windows/desktop/Midl/strict-context-handle) attribute for all context handles.</span></span> <span data-ttu-id="c6c31-105">Por padrão, um identificador de contexto não está associado a uma interface, o que significa que um identificador de contexto pode ser criado em uma interface e, em seguida, passado para outro.</span><span class="sxs-lookup"><span data-stu-id="c6c31-105">By default, a context handle is not associated with an interface, which means a context handle can be created on one interface, then passed to another.</span></span> <span data-ttu-id="c6c31-106">Esse é um ataque fácil, e muitos servidores RPC falham ao impedi-lo.</span><span class="sxs-lookup"><span data-stu-id="c6c31-106">This is an easy attack, and many RPC servers fail to prevent it.</span></span>

<span data-ttu-id="c6c31-107">Se duas interfaces coexistirem no mesmo processo, um invasor poderá abrir um identificador de contexto em uma interface e passá-lo para outro, o que pode passar pelos dados inesperados.</span><span class="sxs-lookup"><span data-stu-id="c6c31-107">If two interfaces coexist in the same process, an attacker can open a context handle on one interface, and pass it to another, which may trip over the unexpected data.</span></span> <span data-ttu-id="c6c31-108">Muitos desenvolvedores acreditam que seu software é a única interface em um processo, mas com muita frequência que não é o caso, porque o sistema e muitos componentes de terceiros usam o RPC internamente, e essas interfaces podem criar identificadores de contexto.</span><span class="sxs-lookup"><span data-stu-id="c6c31-108">Many developers believe their software is the only interface in a process, but very often that is not the case, because both the system and many third-party components use RPC internally, and those interfaces can create context handles.</span></span> <span data-ttu-id="c6c31-109">Quando o atributo de [**\_ \_ identificador de contexto estrito**](/windows/desktop/Midl/strict-context-handle) é usado, o tempo de execução de RPC garante que um contexto criado em uma interface possa ser passado como um argumento somente para métodos dessa interface.</span><span class="sxs-lookup"><span data-stu-id="c6c31-109">When the [**strict\_context\_handle**](/windows/desktop/Midl/strict-context-handle) attribute is used, the RPC run time ensures that a context created on one interface can be passed as an argument only to methods of that interface.</span></span>

<span data-ttu-id="c6c31-110">Em situações em que um aplicativo é desenvolvido para o Windows Vista, o uso do [**\_ identificador de \_ contexto \_ de tipo estrito**](/windows/desktop/Midl/type-strict-context-handle) está disponível e recomendado.</span><span class="sxs-lookup"><span data-stu-id="c6c31-110">In situations where an application is developed for Windows Vista, the use of [**type\_strict\_context\_handle**](/windows/desktop/Midl/type-strict-context-handle) is available and recommended.</span></span> <span data-ttu-id="c6c31-111">Os identificadores de contexto não são associados a um tipo específico por padrão.</span><span class="sxs-lookup"><span data-stu-id="c6c31-111">Context handles are not associated with a specific type by default.</span></span> <span data-ttu-id="c6c31-112">Quando vários tipos de identificadores de contexto são usados no mesmo processo, é possível que um cliente mal-intencionado passe um identificador de contexto em vez de outro para produzir resultados indesejáveis.</span><span class="sxs-lookup"><span data-stu-id="c6c31-112">When multiple types of context handles are used in the same process, it is possible for a malicious client to pass a context handle in place of another to produce undesirable results.</span></span> <span data-ttu-id="c6c31-113">O uso do **\_ identificador de \_ contexto \_ de tipo estrito** permite que os aplicativos imponham a consistência de tipo de identificador de contexto e evitem qualquer uso de tipo de identificador de contexto incompatível.</span><span class="sxs-lookup"><span data-stu-id="c6c31-113">The use of **type\_strict\_context\_handle** allows applications to enforce context handle type consistency and prevent any mismatched context handle type usage.</span></span>

 

 