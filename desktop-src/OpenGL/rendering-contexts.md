---
title: Contextos de renderização
description: Um contexto de renderização OpenGL é uma porta pela qual todos os comandos OpenGL são aprovados. Cada thread que faz chamadas OpenGL deve ter um contexto de renderização atual. Os contextos de renderização vinculam OpenGL aos sistemas de janelas do Windows.
ms.assetid: 9fbbb0be-2db4-4bfc-9a5c-a43d71554abc
keywords:
- OpenGL no Windows, contextos de renderização
- contextos de renderização OpenGL
ms.topic: article
ms.date: 05/31/2018
ms.openlocfilehash: 53ac2ac6c5948da2b7372d600377666cd9e4e074
ms.sourcegitcommit: 2d531328b6ed82d4ad971a45a5131b430c5866f7
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 09/16/2019
ms.locfileid: "104292435"
---
# <a name="rendering-contexts"></a><span data-ttu-id="69b41-107">Contextos de renderização</span><span class="sxs-lookup"><span data-stu-id="69b41-107">Rendering Contexts</span></span>

<span data-ttu-id="69b41-108">Um *contexto de renderização* OpenGL é uma porta pela qual todos os comandos OpenGL são aprovados.</span><span class="sxs-lookup"><span data-stu-id="69b41-108">An OpenGL *rendering context* is a port through which all OpenGL commands pass.</span></span> <span data-ttu-id="69b41-109">Cada thread que faz chamadas OpenGL deve ter um contexto de renderização atual.</span><span class="sxs-lookup"><span data-stu-id="69b41-109">Every thread that makes OpenGL calls must have a current rendering context.</span></span> <span data-ttu-id="69b41-110">Os contextos de renderização vinculam OpenGL aos sistemas de janelas do Windows.</span><span class="sxs-lookup"><span data-stu-id="69b41-110">Rendering contexts link OpenGL to the Windows windowing systems.</span></span>

<span data-ttu-id="69b41-111">Um aplicativo especifica um contexto de dispositivo do Windows quando ele cria um contexto de renderização.</span><span class="sxs-lookup"><span data-stu-id="69b41-111">An application specifies a Windows device context when it creates a rendering context.</span></span> <span data-ttu-id="69b41-112">Esse contexto de renderização é adequado para desenhar no dispositivo referenciado pelo contexto do dispositivo especificado.</span><span class="sxs-lookup"><span data-stu-id="69b41-112">This rendering context is suitable for drawing on the device that the specified device context references.</span></span> <span data-ttu-id="69b41-113">Em particular, o contexto de renderização tem o mesmo formato de pixel que o contexto do dispositivo.</span><span class="sxs-lookup"><span data-stu-id="69b41-113">In particular, the rendering context has the same pixel format as the device context.</span></span> <span data-ttu-id="69b41-114">Para obter mais informações, consulte [processando funções de contexto](rendering-context-functions.md).</span><span class="sxs-lookup"><span data-stu-id="69b41-114">For more information, see [Rendering Context Functions](rendering-context-functions.md).</span></span>

<span data-ttu-id="69b41-115">Apesar dessa relação, um contexto de renderização não é o mesmo que um contexto de dispositivo.</span><span class="sxs-lookup"><span data-stu-id="69b41-115">Despite this relationship, a rendering context is not the same as a device context.</span></span> <span data-ttu-id="69b41-116">Um contexto de dispositivo contém informações pertinentes ao componente de gráficos (GDI) do Windows.</span><span class="sxs-lookup"><span data-stu-id="69b41-116">A device context contains information pertinent to the graphics component (GDI) of Windows.</span></span> <span data-ttu-id="69b41-117">Um contexto de renderização contém informações pertinentes ao OpenGL.</span><span class="sxs-lookup"><span data-stu-id="69b41-117">A rendering context contains information pertinent to OpenGL.</span></span> <span data-ttu-id="69b41-118">Um contexto de dispositivo deve ser especificado explicitamente em uma chamada GDI.</span><span class="sxs-lookup"><span data-stu-id="69b41-118">A device context must be explicitly specified in a GDI call.</span></span> <span data-ttu-id="69b41-119">Um contexto de renderização é implícito em uma chamada OpenGL.</span><span class="sxs-lookup"><span data-stu-id="69b41-119">A rendering context is implicit in an OpenGL call.</span></span> <span data-ttu-id="69b41-120">Você deve definir o formato de pixel de um contexto de dispositivo antes de criar um contexto de renderização.</span><span class="sxs-lookup"><span data-stu-id="69b41-120">You should set a device context's pixel format before creating a rendering context.</span></span>

<span data-ttu-id="69b41-121">Um thread que faz chamadas OpenGL deve ter um contexto de renderização atual.</span><span class="sxs-lookup"><span data-stu-id="69b41-121">A thread that makes OpenGL calls must have a current rendering context.</span></span> <span data-ttu-id="69b41-122">Se um aplicativo fizer chamadas OpenGL de um thread que não tem um contexto de renderização atual, nada acontecerá; a chamada não tem nenhum efeito.</span><span class="sxs-lookup"><span data-stu-id="69b41-122">If an application makes OpenGL calls from a thread that lacks a current rendering context, nothing happens; the call has no effect.</span></span> <span data-ttu-id="69b41-123">Um aplicativo normalmente cria um contexto de renderização, define-o como o contexto de renderização atual de um thread e, em seguida, chama as funções OpenGL.</span><span class="sxs-lookup"><span data-stu-id="69b41-123">An application commonly creates a rendering context, sets it as a thread's current rendering context, and then calls OpenGL functions.</span></span> <span data-ttu-id="69b41-124">Quando termina de chamar as funções OpenGL, o aplicativo separa o contexto de renderização do thread e, em seguida, exclui o contexto de renderização.</span><span class="sxs-lookup"><span data-stu-id="69b41-124">When it finishes calling OpenGL functions, the application uncouples the rendering context from the thread, and then deletes the rendering context.</span></span> <span data-ttu-id="69b41-125">Uma janela pode ter vários contextos de renderização desenhando ao mesmo tempo, mas um thread pode ter apenas um contexto de renderização ativo atual.</span><span class="sxs-lookup"><span data-stu-id="69b41-125">A window can have multiple rendering contexts drawing to it at one time, but a thread can have only one current, active rendering context.</span></span>

<span data-ttu-id="69b41-126">Um contexto de renderização atual tem um contexto de dispositivo associado.</span><span class="sxs-lookup"><span data-stu-id="69b41-126">A current rendering context has an associated device context.</span></span> <span data-ttu-id="69b41-127">Esse contexto de dispositivo não precisa ser o mesmo contexto de dispositivo usado quando o contexto de renderização foi criado, mas ele deve referenciar o mesmo dispositivo e ter o mesmo formato de pixel.</span><span class="sxs-lookup"><span data-stu-id="69b41-127">That device context need not be the same device context as that used when the rendering context was created, but it must reference the same device and have the same pixel format.</span></span>

<span data-ttu-id="69b41-128">Um thread pode ter apenas um contexto de renderização atual.</span><span class="sxs-lookup"><span data-stu-id="69b41-128">A thread can have only one current rendering context.</span></span> <span data-ttu-id="69b41-129">Um contexto de renderização pode ser atual para apenas um thread.</span><span class="sxs-lookup"><span data-stu-id="69b41-129">A rendering context can be current to only one thread.</span></span>

 

 




