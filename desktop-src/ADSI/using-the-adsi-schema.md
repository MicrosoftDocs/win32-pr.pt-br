---
title: Usando o esquema ADSI
description: Um esquema define o universo de objetos armazenados em um diretório.
ms.assetid: 140a5dd0-6f50-4f84-8708-45c0f1c6bdc4
ms.tgt_platform: multiple
keywords:
- Usando o esquema ADSI
- ADSI, ADSI, usando, esquema ADSI
ms.topic: article
ms.date: 05/31/2018
ms.openlocfilehash: 478104a24d850f79cc52473f3d9e546936c56650
ms.sourcegitcommit: b0ebdefc3dcd5c04bede94091833aa1015a2f95c
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 08/21/2020
ms.locfileid: "103823988"
---
# <a name="using-the-adsi-schema"></a><span data-ttu-id="c5183-105">Usando o esquema ADSI</span><span class="sxs-lookup"><span data-stu-id="c5183-105">Using the ADSI Schema</span></span>

<span data-ttu-id="c5183-106">Um esquema define o universo de objetos armazenados em um diretório.</span><span class="sxs-lookup"><span data-stu-id="c5183-106">A schema defines the universe of objects stored in a directory.</span></span> <span data-ttu-id="c5183-107">No Active Directory, o esquema especifica quais atributos um objeto de serviço de diretório pode ou deve ter.</span><span class="sxs-lookup"><span data-stu-id="c5183-107">In Active Directory, the schema specifies what attributes a directory service object can, or must, have.</span></span> <span data-ttu-id="c5183-108">Ele também especifica o intervalo de valores e a sintaxe dos atributos e se eles dão suporte a valores únicos ou múltiplos.</span><span class="sxs-lookup"><span data-stu-id="c5183-108">It also specifies the value range and the syntax of the attributes and whether they support single or multiple values.</span></span> <span data-ttu-id="c5183-109">Em suma, o esquema é organizado por definições de classe, definições de atributo e sintaxe de atributo.</span><span class="sxs-lookup"><span data-stu-id="c5183-109">In short, the schema is organized by class definitions, attribute definitions, and attribute syntax.</span></span> <span data-ttu-id="c5183-110">A ADSI fornece três interfaces para ler dados de atributo, classe e sintaxe de um esquema: [**IADsClass**](/windows/desktop/api/Iads/nn-iads-iadsclass), [**iadsproperty**](/windows/desktop/api/Iads/nn-iads-iadsproperty)e [**IADsSyntax**](/windows/desktop/api/Iads/nn-iads-iadssyntax).</span><span class="sxs-lookup"><span data-stu-id="c5183-110">ADSI provides three interfaces for reading attribute, class, and syntax data from a schema: [**IADsClass**](/windows/desktop/api/Iads/nn-iads-iadsclass), [**IADsProperty**](/windows/desktop/api/Iads/nn-iads-iadsproperty), and [**IADsSyntax**](/windows/desktop/api/Iads/nn-iads-iadssyntax).</span></span>

<span data-ttu-id="c5183-111">Active Directory usa um conjunto de objetos de esquema para fornecer gerenciamento de esquema extensível dinamicamente.</span><span class="sxs-lookup"><span data-stu-id="c5183-111">Active Directory uses a set of schema objects to provide dynamically extensible schema management.</span></span> <span data-ttu-id="c5183-112">Para obter mais informações sobre um objeto desconhecido, pesquise seus objetos de esquema associados.</span><span class="sxs-lookup"><span data-stu-id="c5183-112">For more information about an unknown object, look up its associated schema objects.</span></span> <span data-ttu-id="c5183-113">Para criar uma nova definição de classe ou estender uma definição de classe existente, você pode criar ou estender os objetos de esquema apropriados.</span><span class="sxs-lookup"><span data-stu-id="c5183-113">To create a new class definition or extend an existing class definition, you can create or extend the appropriate schema objects.</span></span> <span data-ttu-id="c5183-114">Os objetos de esquema são organizados no contêiner de esquema de um determinado diretório.</span><span class="sxs-lookup"><span data-stu-id="c5183-114">Schema objects are organized in the schema container of a given directory.</span></span> <span data-ttu-id="c5183-115">Para acessar uma classe de esquema de objeto, use a propriedade [**IADs. Schema**](iads-property-methods.md) do objeto para obter a cadeia de caracteres ADsPath e use essa cadeia de caracteres para associar a uma interface [**IADsClass**](/windows/desktop/api/Iads/nn-iads-iadsclass) na classe de esquema de objeto.</span><span class="sxs-lookup"><span data-stu-id="c5183-115">To access an object schema class, use the [**IADs.Schema**](iads-property-methods.md) property of the object to obtain the ADsPath string and use that string to bind to an [**IADsClass**](/windows/desktop/api/Iads/nn-iads-iadsclass) interface on the object schema class.</span></span>

<span data-ttu-id="c5183-116">Para determinar as definições de atributo, ou seja, o intervalo de valores, a sintaxe e assim por diante, inspecione os objetos de atributo de esquema para cada propriedade com suporte no objeto de serviço de diretório.</span><span class="sxs-lookup"><span data-stu-id="c5183-116">To determine attribute definitions, that is, the range of values, the syntax, and so on, inspect the schema attribute objects for each property supported by the directory service object.</span></span> <span data-ttu-id="c5183-117">Para obter mais informações sobre como acessar os objetos de atributo de esquema, consulte [**iadsproperty**](/windows/desktop/api/Iads/nn-iads-iadsproperty).</span><span class="sxs-lookup"><span data-stu-id="c5183-117">For more information about how to access the schema attribute objects, see [**IADsProperty**](/windows/desktop/api/Iads/nn-iads-iadsproperty).</span></span>

<span data-ttu-id="c5183-118">A ADSI abstrai os dados de sintaxe conforme necessário e permite que você use [**IADsSyntax**](/windows/desktop/api/Iads/nn-iads-iadssyntax) para identificar a sintaxe necessária para representar dados de objeto.</span><span class="sxs-lookup"><span data-stu-id="c5183-118">ADSI abstracts the syntax data as necessary and enables you to use [**IADsSyntax**](/windows/desktop/api/Iads/nn-iads-iadssyntax) to identify the syntax required to represent object data.</span></span>

<span data-ttu-id="c5183-119">Para obter mais informações sobre o esquema de Active Directory, consulte [Active Directory esquema](/windows/desktop/AD/active-directory-schema).</span><span class="sxs-lookup"><span data-stu-id="c5183-119">For more information about the Active Directory schema, see [Active Directory Schema](/windows/desktop/AD/active-directory-schema).</span></span> <span data-ttu-id="c5183-120">Para obter exemplos de código a serem usados para ler o contêiner de esquema, consulte [lendo o esquema](reading-the-schema.md).</span><span class="sxs-lookup"><span data-stu-id="c5183-120">For code examples to use to read the schema container, see [Reading the Schema](reading-the-schema.md).</span></span>

 

 