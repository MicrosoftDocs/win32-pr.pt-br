---
description: IDs de cadeia de caracteres de recurso definidas pelo chamador a serem retornadas em dados xml para visualizar objetos.
MS-HAID: vspixengine.Xml\_Resource\_Ids
MSHAttr:
- PreferredSiteName:MSDN
- PreferredLib:/library/windows/desktop
title: Xml_Resource_Ids enumeração
ms.topic: reference
ms.date: 05/31/2018
ms.assetid: 1F2ECC78-73EC-4A59-8CF7-1BB7FB80244E
api_name:
- Xml_Resource_Ids
api_type:
- HeaderDef
api_location:
- vspixengine.h
topic_type:
- APIRef
- kbSyntax
ms.openlocfilehash: 6cde2fe260924702eaeaebad235ebf1b469c73ed
ms.sourcegitcommit: c276a8912787b2cda74dcf54eb96df961bb1188b
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 08/20/2021
ms.locfileid: "122621902"
---
# <a name="span-idvspixenginexml_resource_idsspanxml_resource_ids-enumeration"></a><span id="vspixengine.xml_resource_ids"></span>\_ \_ Enumeração de IDs de recurso xml

IDs de cadeia de caracteres de recurso definidas pelo chamador a serem retornadas em dados xml para visualizar objetos

## <a name="syntax"></a>Sintaxe


```C++
};
```

## <a name="constants"></a>Constantes

<span id="XML_DEVICE_INFORMATION"></span><span id="xml_device_information"></span>**INFORMAÇÕES \_ DO DISPOSITIVO \_ XML**  
ID do recurso para a cadeia de caracteres "informações do dispositivo" usada em dados XML.

<span id="XML_IA_STATE"></span><span id="xml_ia_state"></span>**ESTADO \_ IA \_ XML**  
ID do recurso para a cadeia de caracteres "assembler de entrada" usada em dados XML.

<span id="XML_VS_STATE"></span><span id="xml_vs_state"></span>**XML \_ VS \_ STATE**  
ID do recurso para a cadeia de caracteres "estágio do sombreador de vértice" usada em dados XML.

<span id="XML_GS_STATE"></span><span id="xml_gs_state"></span>**XML \_ GS \_ STATE**  
ID do recurso para a cadeia de caracteres "estágio de sombreador geometry" usado em dados XML.

<span id="XML_RASTERIZER_STATE"></span><span id="xml_rasterizer_state"></span>**ESTADO \_ DO RASTERIZADOR \_ XML**  
ID do recurso para a cadeia de caracteres "estágio do rasterizador" usado em dados XML.

<span id="XML_PS_STATE"></span><span id="xml_ps_state"></span>**XML \_ PS \_ STATE**  
ID do recurso para a cadeia de caracteres "estágio do sombreador de pixel" usada em dados XML.

<span id="XML_OM_STATE"></span><span id="xml_om_state"></span>**ESTADO \_ DO XML OM \_**  
ID do recurso para a cadeia de caracteres "estágio de fusão de saída" usada em dados XML.

<span id="XML_HS_STATE"></span><span id="xml_hs_state"></span>**ESTADO \_ DE HS \_ XML**  
ID do recurso para a cadeia de caracteres "estágio do sombreador de chassi" usado em dados XML.

<span id="XML_DS_STATE"></span><span id="xml_ds_state"></span>**XML \_ DS \_ STATE**  
ID do recurso para a cadeia de caracteres "estágio do sombreador de domínio" usada em dados XML.

<span id="XML_CS_STATE"></span><span id="xml_cs_state"></span>**XML \_ CS \_ STATE**  
ID do recurso para a cadeia de caracteres "estágio do sombreador de computação" usada em dados XML.

<span id="XML_CLASS_INSTANCE_11"></span><span id="xml_class_instance_11"></span>**INSTÂNCIA \_ DE CLASSE XML \_ \_ 11**  
ID do recurso para cadeia de caracteres "instâncias de classe" usada em dados XML.

<span id="XML_DEVICE_10"></span><span id="xml_device_10"></span>**DISPOSITIVO XML \_ \_ 10**  
ID do recurso para a cadeia de caracteres "dispositivo 10" usada em dados XML.

<span id="XML_INPUT_LAYOUT_11"></span><span id="xml_input_layout_11"></span>**LAYOUT \_ DE ENTRADA XML \_ \_ 11**  
ID do recurso para a cadeia de caracteres "layout de entrada 11" usada em dados XML.

<span id="XML_SAMPLER_STATE_11"></span><span id="xml_sampler_state_11"></span>**XML \_ SAMPLER \_ STATE \_ 11**  
ID do recurso para a cadeia de caracteres "sampler state 11" usada em dados XML.

<span id="XML_RASTER_STATE_11"></span><span id="xml_raster_state_11"></span>**XML \_ RASTER \_ STATE \_ 11**  
ID do recurso para cadeia de caracteres "estado de raster 11" usado em dados XML.

<span id="XML_BLEND_STATE_11"></span><span id="xml_blend_state_11"></span>**XML \_ BLEND \_ STATE \_ 11**  
ID do recurso para a cadeia de caracteres "blend state 11" usada em dados XML.

<span id="XML_DEPTH_STENCIL_STATE_11"></span><span id="xml_depth_stencil_state_11"></span>**XML \_ DEPTH \_ STENCIL \_ STATE \_ 11**  
ID do recurso para a cadeia de caracteres "estado de estêncil de profundidade 11" usado em dados XML.

<span id="XML_UNORDERED_ACCESS_VIEW_11"></span><span id="xml_unordered_access_view_11"></span>**EXIBIÇÃO DE ACESSO NÃO ORGANIZADO XML \_ \_ \_ \_ 11**  
ID do recurso para a cadeia de caracteres "exibição de acesso não organizado 11" usada em dados XML.

<span id="XML_SHADER_RESOURCE_VIEW_11"></span><span id="xml_shader_resource_view_11"></span>**EXIBIÇÃO \_ DE RECURSO DO \_ SOMBREADOR XML \_ \_ 11**  
ID do recurso para a cadeia de caracteres "exibição de recurso do sombreador 11" usada em dados XML.

<span id="XML_DEPTH_STENCIL_VIEW_11"></span><span id="xml_depth_stencil_view_11"></span>**EXIBIÇÃO DE ESTÊNCIL DE PROFUNDIDADE \_ \_ XML \_ \_ 11**  
ID do recurso para a cadeia de caracteres "exibição de estêncil de profundidade 11" usada em dados XML.

<span id="XML_RENDER_TARGET_VIEW_11"></span><span id="xml_render_target_view_11"></span>**EXIBIÇÃO DE DESTINO DE RENDERIZAÇÃO XML \_ \_ \_ \_ 11**  
ID do recurso para a cadeia de caracteres "renderizar exibição de destino 11" usada em dados XML.

<span id="XML_DEVICE_11"></span><span id="xml_device_11"></span>**DISPOSITIVO XML \_ \_ 11**  
ID do recurso para a cadeia de caracteres "dispositivo 11" usada em dados XML.

<span id="XML_SWAP_CHAIN"></span><span id="xml_swap_chain"></span>**CADEIA DE \_ PERMUTA \_ XML**  
ID do recurso para cadeia de caracteres "cadeia de permuta" usada em dados XML.

<span id="XML_INPUT_LAYOUT_10"></span><span id="xml_input_layout_10"></span>**LAYOUT \_ DE ENTRADA XML \_ \_ 10**  
ID do recurso para a cadeia de caracteres "layout de entrada 10" usada em dados XML.

<span id="XML_SAMPLER_STATE_10"></span><span id="xml_sampler_state_10"></span>**XML \_ SAMPLER \_ STATE \_ 10**  
ID do recurso para a cadeia de caracteres "sampler state 10" usada em dados XML.

<span id="XML_RASTER_STATE_10"></span><span id="xml_raster_state_10"></span>**XML \_ RASTER \_ STATE \_ 10**  
ID do recurso para a cadeia de caracteres "estado de raster 10" usada em dados XML.

<span id="XML_BLEND_STATE_10"></span><span id="xml_blend_state_10"></span>**XML \_ BLEND \_ STATE \_ 10**  
ID do recurso para a cadeia de caracteres "blend state 10" usada em dados XML.

<span id="XML_DEPTH_STENCIL_STATE_10"></span><span id="xml_depth_stencil_state_10"></span>**ESTADO \_ \_ DE ESTÊNCIL DE PROFUNDIDADE \_ XML \_ 10**  
ID do recurso para a cadeia de caracteres "estado de estêncil de profundidade 10" usada em dados XML.

<span id="XML_SHADER_RESOURCE_VIEW_10"></span><span id="xml_shader_resource_view_10"></span>**EXIBIÇÃO DE RECURSO DO SOMBREADOR XML \_ \_ \_ \_ 10**  
ID do recurso para a cadeia de caracteres "exibição de recurso do sombreador 10" usada em dados XML.

<span id="XML_DEPTH_STENCIL_VIEW_10"></span><span id="xml_depth_stencil_view_10"></span>**EXIBIÇÃO \_ DE ESTÊNCIL DE PROFUNDIDADE XML \_ \_ \_ 10**  
ID do recurso para a cadeia de caracteres "exibição de estêncil de profundidade 10" usada em dados XML.

<span id="XML_RENDER_TARGET_VIEW_10"></span><span id="xml_render_target_view_10"></span>**EXIBIÇÃO DE DESTINO DE RENDERIZAÇÃO XML \_ \_ \_ \_ 10**  
ID do recurso para a cadeia de caracteres "renderizar exibição de destino 10" usada em dados XML.

<span id="XML_VERTEX_DECLARATION"></span><span id="xml_vertex_declaration"></span>**DECLARAÇÃO \_ DE VÉRTICE \_ XML**  
ID do recurso para a cadeia de caracteres "declaração de vértice" usada em dados XML.

<span id="XML_STATE_BLOCK_STATE_10"></span><span id="xml_state_block_state_10"></span>**ESTADO \_ DO BLOCO DE ESTADO XML \_ \_ \_ 10**  
ID do recurso para a cadeia de caracteres "estado de bloco de estado 10" usado em dados XML.

<span id="XML_DEVICE_CONTEXT_STATE_11"></span><span id="xml_device_context_state_11"></span>**ESTADO DE CONTEXTO DO DISPOSITIVO XML \_ \_ \_ \_ 11**  
ID do recurso para a cadeia de caracteres "estado de contexto do dispositivo 11" usada em dados XML.

<span id="XML_DEVICE_STATE_10"></span><span id="xml_device_state_10"></span>**ESTADO \_ DO DISPOSITIVO XML \_ \_ 10**  
ID do recurso para a cadeia de caracteres "estado do dispositivo 10" usada em dados XML.

<span id="XML_BACK_FACE"></span><span id="xml_back_face"></span>**XML \_ BACK \_ FACE**  
ID do recurso para a cadeia de caracteres "back face" usada em dados XML.

<span id="XML_FRONT_FACE"></span><span id="xml_front_face"></span>**XML \_ FRONT \_ FACE**  
ID do recurso para a cadeia de caracteres "front face" usada em dados XML.

<span id="XML_DEVICE_CREATION_INFORMATION"></span><span id="xml_device_creation_information"></span>**INFORMAÇÕES DE \_ CRIAÇÃO \_ DE DISPOSITIVO \_ XML**  
ID do recurso para a cadeia de caracteres "informações de criação de dispositivo" usada em dados XML.

<span id="XML_OTHER_INFORMATION"></span><span id="xml_other_information"></span>**XML \_ OUTRAS \_ INFORMAÇÕES**  
ID do recurso para a cadeia de caracteres "outras informações" usadas em dados XML.

<span id="XML_MULTISAMPLING"></span><span id="xml_multisampling"></span>**XML \_ MULTISAMPLING**  
ID do recurso para a cadeia de caracteres "multisampling" usada em dados XML.

<span id="XML_BACK_BUFFER"></span><span id="xml_back_buffer"></span>**BUFFER \_ DE RETORNO \_ XML**  
ID do recurso para a cadeia de caracteres "buffer de fundo" usada em dados XML.

<span id="XML_GENERAL_INFORMATION"></span><span id="xml_general_information"></span>**INFORMAÇÕES \_ GERAIS \_ DE XML**  
ID do recurso para a cadeia de caracteres "informações gerais" usada em dados XML.

<span id="XML_RENDER_TARGETS"></span><span id="xml_render_targets"></span>**DESTINOS \_ \_ DE RENDERIZAÇÃO XML**  
ID do recurso para a cadeia de caracteres "destinos de renderização" usados em dados XML.

<span id="XML_BLEND_DESCRIPTION"></span><span id="xml_blend_description"></span>**DESCRIÇÃO \_ DO BLEND \_ XML**  
ID do recurso para a cadeia de caracteres "blend description" usada em dados XML.

<span id="XML_DEPTH_STENCIL"></span><span id="xml_depth_stencil"></span>**\_ESTÊNCIL DE \_ PROFUNDIDADE XML**  
ID do recurso para a cadeia de caracteres "estêncil de profundidade" usada em dados XML.

<span id="XML_FRONT_FACE_STENCIL"></span><span id="xml_front_face_stencil"></span>**\_ESTÊNCIL DE FACE FRONTAL \_ \_ XML**  
ID do recurso para cadeia de caracteres "estêncil de rosto frontal" usado em dados XML.

<span id="XML_BACK_FACE_STENCIL"></span><span id="xml_back_face_stencil"></span>**\_ESTÊNCIL DE FACE TRASEIRA \_ \_ XML**  
ID do recurso para cadeia de caracteres "estêncil de face traseira" usado em dados XML.

<span id="XML_DEVICE1_CREATION_INFORMATION"></span><span id="xml_device1_creation_information"></span>**\_Informações de \_ criação de DEVICE1 XML \_**  
ID do recurso para a cadeia de caracteres "informações de criação de Device1" usadas em dados XML.

<span id="XML_VIEWPORTS"></span><span id="xml_viewports"></span>**\_visores XML**  
ID de recurso para cadeias de caracteres "viewports" usadas em dados XML.

<span id="XML_SCISSOR_RECTS"></span><span id="xml_scissor_rects"></span>**retângulos de corte XML \_ \_**  
ID de recurso para cadeia de caracteres "molas de recorte" usada em dados XML.

<span id="XML_RASTERIZER"></span><span id="xml_rasterizer"></span>**\_rasterizador XML**  
ID de recurso da cadeia de caracteres "rasterizador" usada em dados XML.

<span id="XML_SHADER_GENERAL_INFORMATION"></span><span id="xml_shader_general_information"></span>**\_informações gerais do sombreador XML \_ \_**  
ID do recurso para cadeia de caracteres "informações gerais do sombreador" usadas em dados XML.

<span id="XML_SHADER_RESOURCES"></span><span id="xml_shader_resources"></span>**\_recursos de sombreador XML \_**  
ID de recurso da cadeia de caracteres "recursos do sombreador" usado em dados XML.

<span id="XML_SAMPLER_STATES"></span><span id="xml_sampler_states"></span>**\_Estados de amostra XML \_**  
ID de recurso para cadeia de caracteres "Estados de amostra" usados em dados XML.

<span id="XML_CONSTANT_BUFFERS"></span><span id="xml_constant_buffers"></span>**\_buffers de constantes XML \_**  
ID de recurso para "buffers constantes" da cadeia de caracteres usada em dados XML.

<span id="XML_STREAM_OUTPUT_BUFFERS"></span><span id="xml_stream_output_buffers"></span>**\_ \_ buffers de saída de fluxo XML \_**  
ID de recurso para cadeia de caracteres "buffers de saída de fluxo" usados em dados XML.

<span id="XML_INPUT_LAYOUT_GENERAL_INFORMATION"></span><span id="xml_input_layout_general_information"></span>**\_ \_ informações gerais do layout de entrada XML \_ \_**  
ID do recurso para a cadeia de caracteres "informações gerais do layout de entrada" usadas em dados XML.

<span id="XML_INPUT_LAYOUTS"></span><span id="xml_input_layouts"></span>**\_layouts de entrada XML \_**  
ID de recurso para cadeias de caracteres "layouts de entrada" usados em dados XML.

<span id="XML_VERTEX_BUFFERS"></span><span id="xml_vertex_buffers"></span>**\_buffers de vértice XML \_**  
ID de recurso para "buffers de vértice" da cadeia de caracteres usados em dados XML.

<span id="XML_INDEX_BUFFERS"></span><span id="xml_index_buffers"></span>**\_buffers de índice XML \_**  
ID de recurso para "buffers de índice" da cadeia de caracteres usada em dados XML.

<span id="XML_PRIMITIVE_TOP_GENERAL"></span><span id="xml_primitive_top_general"></span>**\_geral da \_ parte superior XML primitiva \_**  
ID do recurso para a cadeia de caracteres "primitiva geral superior" usada em dados XML.

<span id="XML_BLEND_STATE"></span><span id="xml_blend_state"></span>**\_estado de mesclagem XML \_**  
ID de recurso para a cadeia de caracteres "estado de mistura" usado em dados XML.

<span id="XML_UNORDERED_ACCESS_VIEWS"></span><span id="xml_unordered_access_views"></span>**\_exibições de acesso não ordenado XML \_ \_**  
ID de recurso para cadeias de caracteres "exibições de acesso não ordenado" usada em dados XML.

<span id="XML_CLASS_INSTANCES"></span><span id="xml_class_instances"></span>**\_instâncias de classe XML \_**  
ID de recurso para cadeia de caracteres "instâncias de classe" usadas em dados XML.

<span id="XML_INSTANCE_INDEX"></span><span id="xml_instance_index"></span>**\_índice de instância XML \_**  
ID de recurso da cadeia de caracteres "índice de instância" usada em dados XML.

<span id="XML_INSTANCE_NAME"></span><span id="xml_instance_name"></span>**\_nome da instância XML \_**  
ID de recurso para a cadeia de caracteres "nome da instância" usada em dados XML.

<span id="XML_BASE_SAMPLER"></span><span id="xml_base_sampler"></span>**\_amostra de base XML \_**  
ID do recurso da cadeia de caracteres "base do classificador" usada em dados XML.

<span id="XML_BASE_TEXTURE"></span><span id="xml_base_texture"></span>**\_textura de base XML \_**  
ID do recurso da cadeia de caracteres "textura base" usada em dados XML.

<span id="XML_BASE_CONSTANT_BUFFER_OFFSET"></span><span id="xml_base_constant_buffer_offset"></span>**\_deslocamento de \_ buffer de constante base \_ XML \_**  
ID do recurso da cadeia de caracteres "deslocamento do buffer de constante base" usado em dados XML.

<span id="XML_CONSTANT_BUFFER"></span><span id="xml_constant_buffer"></span>**\_buffer de constantes XML \_**  
ID de recurso para cadeia de caracteres "buffer constante" usado em dados XML.

<span id="XML_TYPE_ID"></span><span id="xml_type_id"></span>**\_ID do tipo XML \_**  
ID do recurso para a cadeia de caracteres "ID do tipo" usada em dados XML.

<span id="XML_INSTANCE_ID"></span><span id="xml_instance_id"></span>**\_ID da instância XML \_**  
ID de recurso da cadeia de caracteres "ID da instância" usada em dados XML.

<span id="XML_TYPE_NAME"></span><span id="xml_type_name"></span>**\_nome do tipo XML \_**  
ID de recurso para a cadeia de caracteres "nome do tipo" usada em dados XML.

<span id="XML_CREATED_BY"></span><span id="xml_created_by"></span>**XML \_ criado \_ por**  
ID de recurso para cadeia de caracteres "criado por" usado em dados XML.

<span id="XML_CLASS_LINKAGE"></span><span id="xml_class_linkage"></span>**\_vinculação de classe XML \_**  
ID de recurso da cadeia de caracteres "ligação de classe" usada em dados XML.

<span id="XML_DESCRIPTION"></span><span id="xml_description"></span>**Descrição de XML \_**  
ID do recurso para a cadeia de caracteres "Descrição" usada em dados XML.

<span id="XML_SEMANTIC_NAME"></span><span id="xml_semantic_name"></span>**\_nome SEMÂNTICO \_ XML**  
ID de recurso para a cadeia de caracteres "nome semântico" usado em dados XML.

<span id="XML_SEMANTIC_INDEX"></span><span id="xml_semantic_index"></span>**\_índice SEMÂNTICO \_ XML**  
ID de recurso para cadeia de caracteres "índice semântico" usado em dados XML.

<span id="XML_FORMAT"></span><span id="xml_format"></span>**\_formato XML**  
ID de recurso para cadeia de caracteres "Format" usada em dados XML.

<span id="XML_INPUT_SLOT"></span><span id="xml_input_slot"></span>**\_slot de entrada XML \_**  
ID de recurso para cadeia de caracteres "slot de entrada" usado em dados XML.

<span id="XML_ALIGNED_BYTE_OFFSET"></span><span id="xml_aligned_byte_offset"></span>**\_deslocamento de \_ byte ALINHAdo XML \_**  
ID do recurso da cadeia de caracteres "deslocamento de byte alinhado" usado em dados XML.

<span id="XML_INPUT_SLOT_CLASS"></span><span id="xml_input_slot_class"></span>**\_classe de \_ slot de entrada XML \_**  
ID de recurso da cadeia de caracteres "classe de slot de entrada" usada em dados XML.

<span id="XML_INSTANCE_DATA_STEP_RATE"></span><span id="xml_instance_data_step_rate"></span>**\_taxa de \_ etapas de dados da instância XML \_ \_**  
ID de recurso da cadeia de caracteres "taxa de etapas de dados da instância" usada em dados XML.

<span id="XML_ADDRESS_U"></span><span id="xml_address_u"></span>**\_endereço XML \_ U**  
ID de recurso para a cadeia de caracteres "address u" usada em dados XML.

<span id="XML_ADDRESS_V"></span><span id="xml_address_v"></span>**\_endereço XML \_ V**  
ID de recurso para a cadeia de caracteres "address v" usada em dados XML.

<span id="XML_ADDRESS_W"></span><span id="xml_address_w"></span>**\_endereço XML \_ W**  
ID do recurso para a cadeia de caracteres "endereço w" usado em dados XML.

<span id="XML_MIN_LOD"></span><span id="xml_min_lod"></span>**\_LOD mín. XML \_**  
ID de recurso da cadeia de caracteres "min LOD" usada em dados XML.

<span id="XML_MAX_LOD"></span><span id="xml_max_lod"></span>**\_LOD máximo de XML \_**  
ID do recurso para a cadeia de caracteres "Max LOD" usada em dados XML.

<span id="XML_FILTER"></span><span id="xml_filter"></span>**\_filtro XML**  
ID de recurso para cadeia de caracteres "filtro" usada em dados XML.

<span id="XML_MIP_SLICE"></span><span id="xml_mip_slice"></span>**\_fatia de MIP XML \_**  
ID de recurso da cadeia de caracteres "fatia MIP" usada em dados XML.

<span id="XML_MOST_DETAILED_MIP"></span><span id="xml_most_detailed_mip"></span>**\_MIP mais \_ detalhado de XML \_**  
ID de recurso da cadeia de caracteres "MIP mais detalhado" usada em dados XML.

<span id="XML_MIP_LEVELS"></span><span id="xml_mip_levels"></span>**\_níveis de MIP XML \_**  
ID de recurso da cadeia de caracteres "níveis de MIP" usados em dados XML.

<span id="XML_FIRST_ARRAY_SLICE"></span><span id="xml_first_array_slice"></span>**\_fatia da primeira \_ matriz \_ XML**  
ID de recurso da cadeia de caracteres "primeira fatia da matriz" usada em dados XML.

<span id="XML_ARRAY_SIZE"></span><span id="xml_array_size"></span>**\_tamanho da matriz XML \_**  
ID de recurso da cadeia de caracteres "tamanho da matriz" usada em dados XML.

<span id="XML_RESOURCE"></span><span id="xml_resource"></span>**\_recurso XML**  
ID do recurso para a cadeia de caracteres "Resource" usada em dados XML.

<span id="XML_RESOURCE_TYPE"></span><span id="xml_resource_type"></span>**\_tipo de recurso XML \_**  
ID do recurso para a cadeia de caracteres "tipo de recurso" usado em dados XML.

<span id="XML_ELEMENT_OFFSET"></span><span id="xml_element_offset"></span>**\_deslocamento do elemento XML \_**  
ID do recurso para a cadeia de caracteres "deslocamento do elemento" usado em dados XML.

<span id="XML_ELEMENT_WIDTH"></span><span id="xml_element_width"></span>**LARGURA \_ DO \_ ELEMENTO XML**  
ID do recurso para a cadeia de caracteres "largura do elemento" usada em dados XML.

<span id="XML_FIRST_WITH_SLICE"></span><span id="xml_first_with_slice"></span>**XML \_ FIRST \_ WITH \_ SLICE**  
ID do recurso para a cadeia de caracteres "primeiro com fatia" usada em dados XML.

<span id="XML_BORDER_COLOR"></span><span id="xml_border_color"></span>**COR \_ DA BORDA \_ XML**  
ID do recurso para a cadeia de caracteres "cor da borda" usada em dados XML.

<span id="XML_SLOT"></span><span id="xml_slot"></span>**XML \_ SLOT**  
ID do recurso para a cadeia de caracteres "slot" usada em dados XML.

<span id="XML_COMPARISON_FUNCTION"></span><span id="xml_comparison_function"></span>**FUNÇÃO \_ DE COMPARAÇÃO \_ XML**  
ID do recurso para a "função de comparação" de cadeia de caracteres usada em dados XML.

<span id="XML_MAX_ANISOTROPY"></span><span id="xml_max_anisotropy"></span>**XML \_ MAX \_ ANISOOSEY**  
ID do recurso para a cadeia de caracteres "max anisoosey" usada em dados XML.

<span id="XML_MIP_LOD_BIAS"></span><span id="xml_mip_lod_bias"></span>**DESVIO \_ DE \_ LOD DA MIP \_ XML**  
ID do recurso para a cadeia de caracteres "desvio de lod mip" usado em dados XML.

<span id="XML_FILL_MODE"></span><span id="xml_fill_mode"></span>**MODO \_ DE PREENCHIMENTO \_ XML**  
ID do recurso para a cadeia de caracteres "modo de preenchimento" usada em dados XML.

<span id="XML_CULL_MODE"></span><span id="xml_cull_mode"></span>**MODO \_ DE \_ RESSUÇÃO XML**  
ID do recurso para a cadeia de caracteres "modo de replicação" usada em dados XML.

<span id="XML_FRONT_COUNTER_CLOCKWISE"></span><span id="xml_front_counter_clockwise"></span>**CONTADOR FRONTAL XML \_ \_ NO SENTIDO \_ HORÁRIO**  
ID do recurso para a cadeia de caracteres "contador frontal no sentido horário" usada em dados XML.

<span id="XML_DEPTH_BIAS"></span><span id="xml_depth_bias"></span>**DESVIO \_ DE PROFUNDIDADE \_ XML**  
ID do recurso para a cadeia de caracteres "desvio de profundidade" usado em dados XML.

<span id="XML_DEPTH_BIAS_CLAMP"></span><span id="xml_depth_bias_clamp"></span>**FIXE \_ DE DESVIO DE PROFUNDIDADE \_ \_ XML**  
ID do recurso para a cadeia de caracteres "fixação de desvio de profundidade" usada em dados XML.

<span id="XML_SLOPE_SCALE_DEPTH_BIAS"></span><span id="xml_slope_scale_depth_bias"></span>**DESVIO DE \_ PROFUNDIDADE DA ESCALA DE \_ \_ INCLINAÇÃO \_ XML**  
ID do recurso para a cadeia de caracteres "desvio de profundidade de escala de inclinação" usado em dados XML.

<span id="XML_DEPTH_CLIP_ENABLE"></span><span id="xml_depth_clip_enable"></span>**\_HABILITAÇÃO DE \_ CLIPE DE \_ PROFUNDIDADE XML**  
ID do recurso para a cadeia de caracteres "habilitação de clipe de profundidade" usada em dados XML.

<span id="XML_SCISSOR_ENABLE"></span><span id="xml_scissor_enable"></span>**XML \_ SCISSOR \_ ENABLE**  
ID do recurso para a cadeia de caracteres "habilitar tesoura" usada em dados XML.

<span id="XML_MULTISAMPLE_ENABLE"></span><span id="xml_multisample_enable"></span>**XML \_ MULTISAMPLE \_ ENABLE**  
ID do recurso para a cadeia de caracteres "multisample enable" usada em dados XML.

<span id="XML_ANTIALIASED_LINE_ENABLE"></span><span id="xml_antialiased_line_enable"></span>**XML \_ ANTIALIASED \_ LINE \_ ENABLE**  
ID do recurso para a cadeia de caracteres "habilitar linha aninhada" usada em dados XML.

<span id="XML_FUNCTION"></span><span id="xml_function"></span>**FUNÇÃO \_ XML**  
ID do recurso para a "função" de cadeia de caracteres usada em dados XML.

<span id="XML_PASS_OP"></span><span id="xml_pass_op"></span>**XML \_ PASS \_ OP**  
ID do recurso para a cadeia de caracteres "pass op" usada em dados XML.

<span id="XML_FAIL_OP"></span><span id="xml_fail_op"></span>**XML \_ FAIL \_ OP**  
ID do recurso para a cadeia de caracteres "fail op" usada em dados XML.

<span id="XML_DEPTH_FAIL_OP"></span><span id="xml_depth_fail_op"></span>**XML \_ DEPTH \_ FAIL \_ OP**  
ID do recurso para a cadeia de caracteres "operação de falha de profundidade" usada em dados XML.

<span id="XML_DEPTH_ENABLE"></span><span id="xml_depth_enable"></span>**XML \_ DEPTH \_ ENABLE**  
ID do recurso para a cadeia de caracteres "habilitar profundidade" usada em dados XML.

<span id="XML_DEPTH_FUNCTION"></span><span id="xml_depth_function"></span>**FUNÇÃO \_ DE PROFUNDIDADE \_ XML**  
ID do recurso para a cadeia de caracteres "função de profundidade" usada em dados XML.

<span id="XML_DEPTH_WRITE_MASK"></span><span id="xml_depth_write_mask"></span>**MÁSCARA DE \_ GRAVAÇÃO \_ DE PROFUNDIDADE \_ XML**  
ID do recurso para a cadeia de caracteres "máscara de gravação de profundidade" usada em dados XML.

<span id="XML_STENCIL_ENABLE"></span><span id="xml_stencil_enable"></span>**XML \_ STENCIL \_ ENABLE**  
ID do recurso para a cadeia de caracteres "estencil enable" usada em dados XML.

<span id="XML_STENCIL_READ_MASK"></span><span id="xml_stencil_read_mask"></span>**MÁSCARA \_ DE LEITURA DE \_ ESTÊNCIL \_ XML**  
ID do recurso para a cadeia de caracteres "máscara de leitura de estêncil" usada em dados XML.

<span id="XML_STENCIL_WRITE_MASK"></span><span id="xml_stencil_write_mask"></span>**MÁSCARA \_ DE GRAVAÇÃO DE ESTÊNCIL \_ \_ XML**  
ID do recurso para a cadeia de caracteres "máscara de gravação de estêncil" usada em dados XML.

<span id="XML_FIRST_ELEMENT"></span><span id="xml_first_element"></span>**ELEMENTO XML \_ \_ FIRST**  
ID do recurso para a cadeia de caracteres "primeiro elemento" usada em dados XML.

<span id="XML_NUM_ELEMENTS"></span><span id="xml_num_elements"></span>**ELEMENTOS \_ NUM \_ XML**  
ID do recurso para a cadeia de caracteres "num elements" usada em dados XML.

<span id="XML_FLAGS"></span><span id="xml_flags"></span>**SINALIZADORES \_ XML**  
ID do recurso para "sinalizadores" de cadeia de caracteres usados em dados XML.

<span id="XML_W_SIZE"></span><span id="xml_w_size"></span>**XML \_ W \_ SIZE**  
ID do recurso para a cadeia de caracteres "w size" usada em dados XML.

<span id="XML_FIRST_2D_ARRAY_FACE"></span><span id="xml_first_2d_array_face"></span>**XML \_ FIRST \_ 2D \_ ARRAY \_ FACE**  
ID do recurso para a cadeia de caracteres "primeira face de matriz 2d" usada em dados XML.

<span id="XML_NUM_CUBES"></span><span id="xml_num_cubes"></span>**CUBOS \_ NUM \_ XML**  
ID do recurso para a cadeia de caracteres "num cubes" usada em dados XML.

<span id="XML_FACTORY"></span><span id="xml_factory"></span>**XML \_ FACTORY**  
ID do recurso para a cadeia de caracteres "factory" usada em dados XML.

<span id="XML_ADAPTER"></span><span id="xml_adapter"></span>**ADAPTADOR \_ XML**  
ID do recurso para a cadeia de caracteres "adapter" usada em dados XML.

<span id="XML_HEIGHT"></span><span id="xml_height"></span>**ALTURA \_ DO XML**  
ID do recurso para a cadeia de caracteres "height" usada em dados XML.

<span id="XML_WIDTH"></span><span id="xml_width"></span>**LARGURA \_ XML**  
ID do recurso para a cadeia de caracteres "width" usada em dados XML.

<span id="XML_CREATION_FLAGS"></span><span id="xml_creation_flags"></span>**SINALIZADORES \_ DE CRIAÇÃO DE XML \_**  
ID do recurso para a cadeia de caracteres "sinalizadores de criação" usados em dados XML.

<span id="XML_SDK_VERSION"></span><span id="xml_sdk_version"></span>**VERSÃO \_ DO SDK \_ XML**  
ID do recurso para a cadeia de caracteres "versão do sdk" usada em dados XML.

<span id="XML_DRIVER_TYPE"></span><span id="xml_driver_type"></span>**TIPO \_ DE DRIVER \_ XML**  
ID do recurso para a cadeia de caracteres "tipo de driver" usada em dados XML.

<span id="XML_FEATURE_LEVEL"></span><span id="xml_feature_level"></span>**NÍVEL \_ DE RECURSO \_ XML**  
ID do recurso para a cadeia de caracteres "nível de recurso" usada em dados XML.

<span id="XML_IMMEDIATE_CONTEXT"></span><span id="xml_immediate_context"></span>**CONTEXTO \_ IMEDIATO \_ XML**  
ID do recurso para a cadeia de caracteres "contexto imediato" usado em dados XML.

<span id="XML_EXCEPTION_MODE"></span><span id="xml_exception_mode"></span>**MODO \_ DE \_ EXCEÇÃO XML**  
ID do recurso para a cadeia de caracteres "modo de exceção" usada em dados XML.

<span id="XML_REFRESH_RATE"></span><span id="xml_refresh_rate"></span>**TAXA DE \_ ATUALIZAÇÃO \_ XML**  
ID do recurso para a cadeia de caracteres "taxa de atualização" usada em dados XML.

<span id="XML_SCANLINE_ORDERING"></span><span id="xml_scanline_ordering"></span>**ORDENAÇÃO \_ DE SCANLINE \_ XML**  
ID do recurso para a cadeia de caracteres "scanline ordering" usada em dados XML.

<span id="XML_SCALING"></span><span id="xml_scaling"></span>**DIMENSIONAMENTO \_ XML**  
ID do recurso para a cadeia de caracteres "dimensionamento" usada em dados XML.

<span id="XML_USAGE"></span><span id="xml_usage"></span>**USO \_ DE XML**  
ID do recurso para a cadeia de caracteres "uso" usada em dados XML.

<span id="XML_BUFFER_COUNT"></span><span id="xml_buffer_count"></span>**CONTAGEM DE \_ BUFFER \_ XML**  
ID do recurso para a cadeia de caracteres "contagem de buffers" usada em dados XML.

<span id="XML_WINDOWED"></span><span id="xml_windowed"></span>**XML \_ WINDOWED**  
ID do recurso para a cadeia de caracteres "em janelas" usada em dados XML.

<span id="XML_SWAP_EFFECT"></span><span id="xml_swap_effect"></span>**EFEITO \_ DE PERMUTA \_ XML**  
ID do recurso para a cadeia de caracteres "efeito de permuta" usada em dados XML.

<span id="XML_STREAM"></span><span id="xml_stream"></span>**fluxo de XML \_**  
ID de recurso para cadeia de caracteres "Stream" usada em dados XML.

<span id="XML_OFFSET"></span><span id="xml_offset"></span>**\_deslocamento XML**  
ID do recurso para a cadeia de caracteres "offset" usada em dados XML.

<span id="XML_TYPE"></span><span id="xml_type"></span>**\_tipo XML**  
ID do recurso para a cadeia de caracteres "tipo" usada em dados XML.

<span id="XML_METHOD"></span><span id="xml_method"></span>**\_método XML**  
ID de recurso para cadeia de caracteres "método" usada em dados XML.

<span id="XML_USAGE_INDEX"></span><span id="xml_usage_index"></span>**\_índice de uso XML \_**  
ID do recurso para a cadeia de caracteres "índice de uso" usado em dados XML.

<span id="XML_BLEND_SOURCE"></span><span id="xml_blend_source"></span>**origem do XML \_ Blend \_**  
ID de recurso da cadeia de caracteres "fonte de mistura" usada em dados XML.

<span id="XML_BLEND_DESTINATION"></span><span id="xml_blend_destination"></span>**destino do XML \_ Blend \_**  
ID de recurso da cadeia de caracteres "destino do Blend" usada em dados XML.

<span id="XML_BLEND_OP"></span><span id="xml_blend_op"></span>**\_operabilidade do XML Blend \_**  
ID de recurso para a cadeia de caracteres "Blend op" usada em dados XML.

<span id="XML_ALPHA_BLEND_SOURCE"></span><span id="xml_alpha_blend_source"></span>**origem de mistura de XML \_ alfa \_ \_**  
ID de recurso da cadeia de caracteres "fonte de combinação alfa" usada em dados XML.

<span id="XML_ALPHA_BLEND_DEST"></span><span id="xml_alpha_blend_dest"></span>**DEST. XML \_ Alpha \_ Blend \_**  
ID de recurso para cadeia de caracteres "Alpha Blend dest" usado em dados XML.

<span id="XML_ALPHA_BLEND_OP"></span><span id="xml_alpha_blend_op"></span>**operação de mistura de XML \_ alfa \_ \_**  
ID de recurso da cadeia de caracteres "Alpha Blend op" usada em dados XML.

<span id="XML_RENDER_TARGET_WRITE_MASK"></span><span id="xml_render_target_write_mask"></span>**\_máscara de \_ gravação de destino de renderização XML \_ \_**  
ID de recurso para cadeia de caracteres "renderizar máscara de gravação de destino" usada em dados XML.

<span id="XML_BLEND_ENABLE"></span><span id="xml_blend_enable"></span>**\_Habilitar XML Blend \_**  
ID de recurso da cadeia de caracteres "habilitar o Blend" usado em dados XML.

<span id="XML_ALPHA_TO_COVERAGE_ENABLE"></span><span id="xml_alpha_to_coverage_enable"></span>**XML \_ alfa \_ para \_ habilitar a cobertura \_**  
ID de recurso da cadeia de caracteres "alfa para habilitar a cobertura" usada em dados XML.

<span id="XML_BLEND_STATE_1"></span><span id="xml_blend_state_1"></span>**\_Estado de mesclagem XML \_ \_ 1**  
ID do recurso da cadeia de caracteres "estado do Blend 1" usado em dados XML.

<span id="XML_INDEPENDENT_BLEND_ENABLE"></span><span id="xml_independent_blend_enable"></span>**\_ \_ habilitação de Blend independente de XML \_**  
ID de recurso da cadeia de caracteres "habilitação de combinação independente" usada em dados XML.

<span id="XML_RENDER_TARGET_NUMBER"></span><span id="xml_render_target_number"></span>**\_número de \_ destino de renderização XML \_**  
ID de recurso para cadeia de caracteres "número de destino de renderização" usado em dados XML.

<span id="XML_BUFFER"></span><span id="xml_buffer"></span>**\_buffer XML**  
ID de recurso para cadeia de caracteres "buffer" usada em dados XML.

<span id="XML_PRIMITIVE_TOPOLOGY"></span><span id="xml_primitive_topology"></span>**\_topologia primitiva de XML \_**  
ID de recurso da cadeia de caracteres "topologia primitiva" usada em dados XML.

<span id="XML_STRIDE"></span><span id="xml_stride"></span>**\_Stride XML**  
ID de recurso para a cadeia de caracteres "Stride" usada em dados XML.

<span id="XML_TOP"></span><span id="xml_top"></span>**\_parte superior do XML**  
ID do recurso para a cadeia de caracteres "Top" usada em dados XML.

<span id="XML_RIGHT"></span><span id="xml_right"></span>**XML \_ à direita**  
ID de recurso para cadeia de caracteres "Right" usada em dados XML.

<span id="XML_BOTTOM"></span><span id="xml_bottom"></span>**\_parte inferior do XML**  
ID de recurso para cadeia de caracteres "inferior" usada em dados XML.

<span id="XML_LEFT"></span><span id="xml_left"></span>**XML \_ à esquerda**  
ID de recurso para a cadeia de caracteres "Left" usada em dados XML.

<span id="XML_TOP_LEFT_X"></span><span id="xml_top_left_x"></span>**XML \_ superior \_ esquerdo \_ X**  
ID do recurso para a cadeia de caracteres "superior esquerda x" usada em dados XML.

<span id="XML_TOP_LEFT_Y"></span><span id="xml_top_left_y"></span>**XML \_ superior \_ esquerdo \_ Y**  
ID de recurso da cadeia de caracteres "superior esquerda y" usada em dados XML.

<span id="XML_MIN_DEPTH"></span><span id="xml_min_depth"></span>**\_profundidade mínima \_ XML**  
ID do recurso para a cadeia de caracteres "profundidade mín." usada em dados XML.

<span id="XML_MAX_DEPTH"></span><span id="xml_max_depth"></span>**\_profundidade máxima de XML \_**  
ID do recurso para a cadeia de caracteres "profundidade máxima" usada em dados XML.

<span id="XML_STENCIL_REFERENCE_VALUE"></span><span id="xml_stencil_reference_value"></span>**\_valor de \_ referência do estêncil XML \_**  
ID do recurso para a cadeia de caracteres "valor de referência do estêncil" usado em dados XML.

<span id="XML_DEPTH_BUFFER"></span><span id="xml_depth_buffer"></span>**\_buffer de profundidade XML \_**  
ID de recurso para cadeia de caracteres "buffer de profundidade" usado em dados XML.

<span id="XML_BLEND_FACTOR"></span><span id="xml_blend_factor"></span>**\_fator de mistura XML \_**  
ID do recurso para a cadeia de caracteres "fator de mistura" usado em dados XML.

<span id="XML_SAMPLE_MASK"></span><span id="xml_sample_mask"></span>**\_máscara de exemplo XML \_**  
ID de recurso para cadeia de caracteres "máscara de exemplo" usada em dados XML.

<span id="XML_DEPTH_STENCIL_STATE"></span><span id="xml_depth_stencil_state"></span>**\_estado do \_ estêncil de profundidade XML \_**  
ID de recurso para "estado de estêncil de profundidade" da cadeia de caracteres usada em dados XML.

<span id="XML_SCISSOR_RECT"></span><span id="xml_scissor_rect"></span>**\_retângulo de tesoura XML \_**  
ID do recurso para a cadeia de caracteres "tesoura retangular" usada em dados XML.

<span id="XML_SAMPLER"></span><span id="xml_sampler"></span>**\_amostra XML**  
ID do recurso para a cadeia de caracteres "amostra" usada em dados XML.

<span id="XML_SHADER"></span><span id="xml_shader"></span>**\_sombreador XML**  
ID de recurso para cadeia de caracteres "Shader" usada em dados XML.

<span id="XML_DEPTH_STENCIL_VIEW"></span><span id="xml_depth_stencil_view"></span>**\_exibição de \_ estêncil de profundidade XML \_**  
ID do recurso para a cadeia de caracteres "exibição do estêncil de profundidade" usada em dados XML.

<span id="XML_SHADER_RESOURCE_VIEW"></span><span id="xml_shader_resource_view"></span>**\_exibição de recursos do sombreador XML \_ \_**  
ID de recurso da cadeia de caracteres "modo de exibição de recurso do sombreador" usado em dados XML.

<span id="XML_INPUT_LAYOUT"></span><span id="xml_input_layout"></span>**\_layout de entrada XML \_**  
ID do recurso para a cadeia de caracteres "layout de entrada" usado em dados XML.

<span id="XML_CLASS_INSTANCE"></span><span id="xml_class_instance"></span>**\_instância da classe XML \_**  
ID de recurso da cadeia de caracteres "instância de classe" usada em dados XML.

<span id="XML_STREAM_OUTPUT_BUFFER"></span><span id="xml_stream_output_buffer"></span>**\_buffer de \_ saída de fluxo XML \_**  
ID de recurso para cadeia de caracteres "buffer de saída de fluxo" usado em dados XML.

<span id="XML_SHADER_RESOURCE"></span><span id="xml_shader_resource"></span>**\_recurso sombreador XML \_**  
ID do recurso para a cadeia de caracteres "recurso do sombreador" usado em dados XML.

<span id="XML_RENDER_TARGET_INSTANCE"></span><span id="xml_render_target_instance"></span>**\_instância de \_ destino de renderização XML \_**  
ID de recurso da cadeia de caracteres "processar instância de destino" usada em dados XML.

<span id="XML_VERTEX"></span><span id="xml_vertex"></span>**\_vértice XML**  
ID de recurso para cadeia de caracteres "Vertex" usada em dados XML.

<span id="XML_VERTEX_BUFFER"></span><span id="xml_vertex_buffer"></span>**\_buffer de vértice XML \_**  
ID de recurso para cadeia de caracteres "buffer de vértice" usado em dados XML.

<span id="XML_INSTANCE"></span><span id="xml_instance"></span>**\_instância XML**  
ID de recurso para a cadeia de caracteres "instance" usada em dados XML.

<span id="XML_VIEWPORT"></span><span id="xml_viewport"></span>**\_visor XML**  
ID do recurso para a cadeia de caracteres "visor" usada em dados XML.

<span id="XML_FIRSTCONSTANT"></span><span id="xml_firstconstant"></span>**\_FIRSTCONSTANT XML**  
ID do recurso para a cadeia de caracteres "firstconstant" usada em dados XML.

<span id="XML_NUMCONSTANTS"></span><span id="xml_numconstants"></span>**\_NUMCONSTANTS XML**  
ID do recurso para a cadeia de caracteres "numconstants" usada em dados XML.

<span id="XML_LOGIC_OP"></span><span id="xml_logic_op"></span>**\_operação lógica de XML \_**  
ID do recurso para a cadeia de caracteres "op lógico" usada em dados XML.

<span id="XML_LOGICOPS_ENABLE"></span><span id="xml_logicops_enable"></span>**\_Habilitar XML LOGICOPS \_**  
ID de recurso da cadeia de caracteres "logicops Enable" usado em dados XML.

<span id="XML_SHADER_FILE"></span><span id="xml_shader_file"></span>**\_arquivo do sombreador XML \_**  
ID do recurso da cadeia de caracteres "arquivo do sombreador" usado em dados XML.

<span id="XML_FILE_NAME"></span><span id="xml_file_name"></span>**\_nome do arquivo XML \_**  
ID do recurso para a cadeia de caracteres "nome do arquivo" usado em dados XML.

<span id="XML_10TEXTURE1D_DESC"></span><span id="xml_10texture1d_desc"></span>**\_10TEXTURE1D \_ desc. xml**  
ID de recurso para a cadeia de caracteres "10texture1d desc" usada em dados XML.

<span id="XML_10TEXTURE2D_DESC"></span><span id="xml_10texture2d_desc"></span>**\_10TEXTURE2D \_ desc. xml**  
ID de recurso para a cadeia de caracteres "10texture2d desc" usada em dados XML.

<span id="XML_10TEXTURE3D_DESC"></span><span id="xml_10texture3d_desc"></span>**\_10TEXTURE3D \_ desc. xml**  
ID de recurso para a cadeia de caracteres "10texture3d desc" usada em dados XML.

<span id="XML_TEXTURE_ARRAYSIZE"></span><span id="xml_texture_arraysize"></span>**\_matrizes de textura XML \_**  
ID de recurso para cadeia de caracteres "Texture intermatrizize" usada em dados XML.

<span id="XML_TEXTURE_MIPLEVEL"></span><span id="xml_texture_miplevel"></span>**\_MIPLEVEL de textura XML \_**  
ID de recurso da cadeia de caracteres "Texture MipLevel" usada em dados XML.

<span id="XML_CPUFLAGS"></span><span id="xml_cpuflags"></span>**\_CPUFLAGS XML**  
ID do recurso para a cadeia de caracteres "cpuflags" usada em dados XML.

<span id="XML_MISCFLAGS"></span><span id="xml_miscflags"></span>**\_MISCFLAGS XML**  
ID do recurso para a cadeia de caracteres "miscflags" usada em dados XML.

<span id="XML_BINDFLAGS"></span><span id="xml_bindflags"></span>**\_BINDFLAGS XML**  
ID do recurso para a cadeia de caracteres "bindflags" usada em dados XML.

<span id="XML_SAMPLEDESC_QUALITY"></span><span id="xml_sampledesc_quality"></span>**\_qualidade de SAMPLEDESC XML \_**  
ID de recurso para a cadeia de caracteres "sampledesc Quality" usada em dados XML.

<span id="XML_SAMPLEDESC_COUNT"></span><span id="xml_sampledesc_count"></span>**\_contagem de SAMPLEDESC XML \_**  
ID de recurso da cadeia de caracteres "contagem de sampledesc" usada em dados XML.

<span id="XML_DEPTH"></span><span id="xml_depth"></span>**\_profundidade XML**  
ID do recurso para a cadeia de caracteres "profundidade" usada em dados XML.

<span id="XML_11TEXTURE1D_DESC"></span><span id="xml_11texture1d_desc"></span>**\_11TEXTURE1D \_ desc. xml**  
ID de recurso para a cadeia de caracteres "11texture1d desc" usada em dados XML.

<span id="XML_11TEXTURE2D_DESC"></span><span id="xml_11texture2d_desc"></span>**\_11TEXTURE2D \_ desc. xml**  
ID de recurso para a cadeia de caracteres "11texture2d desc" usada em dados XML.

<span id="XML_11TEXTURE3D_DESC"></span><span id="xml_11texture3d_desc"></span>**\_11TEXTURE3D \_ desc. xml**  
ID de recurso para a cadeia de caracteres "11texture3d desc" usada em dados XML.

<span id="XML_BYTEWIDTH"></span><span id="xml_bytewidth"></span>**\_BYTEWIDTH XML**  
ID do recurso para a cadeia de caracteres "bytewidth" usada em dados XML.

<span id="XML_10BUFFER_DESC"></span><span id="xml_10buffer_desc"></span>**\_10BUFFER \_ desc. xml**  
ID de recurso para a cadeia de caracteres "10buffer desc" usada em dados XML.

<span id="XML_11BUFFER_DESC"></span><span id="xml_11buffer_desc"></span>**\_11BUFFER \_ desc. xml**  
ID de recurso para a cadeia de caracteres "11buffer desc" usada em dados XML.

<span id="XML_BYTESTRIDE"></span><span id="xml_bytestride"></span>**\_BYTESTRIDE XML**  
ID do recurso para a cadeia de caracteres "bytestride" usada em dados XML.

<span id="XML_STEREO"></span><span id="xml_stereo"></span>**estéreo de XML \_**  
ID de recurso para a cadeia de caracteres "estéreo" usada em dados XML.

<span id="XML_BACKGROUNDCOLOR"></span><span id="xml_backgroundcolor"></span>**\_BACKGROUNDCOLOR XML**  
ID de recurso para a cadeia de caracteres "backgroundcolor" usada em dados XML.

<span id="XML_ALPHA_MODE"></span><span id="xml_alpha_mode"></span>**\_modo alfa \_ XML**  
ID do recurso para a cadeia de caracteres "modo alfa" usado em dados XML.

<span id="XML_RESTRICTTOOUTPUT"></span><span id="xml_restricttooutput"></span>**\_RESTRICTTOOUTPUT XML**  
ID do recurso para a cadeia de caracteres "restricttooutput" usada em dados XML.

<span id="XML_MODEROTATION"></span><span id="xml_moderotation"></span>**\_MODEROTATION XML**  
ID do recurso para a cadeia de caracteres "moderotation" usada em dados XML.

<span id="XML_ISTEMPORARYMONOSUPPORTED"></span><span id="xml_istemporarymonosupported"></span>**\_ISTEMPORARYMONOSUPPORTED XML**  
ID do recurso para a cadeia de caracteres "istemporarymonosupported" usada em dados XML.

<span id="XML_FORCED_SAMPLE_COUNT"></span><span id="xml_forced_sample_count"></span>**contagem de amostras de XML \_ forçada \_ \_**  
ID de recurso da cadeia de caracteres "contagem de amostras forçada" usada em dados XML.

<span id="XML_QUERY"></span><span id="xml_query"></span>**\_consulta XML**  
ID de recurso da cadeia de caracteres "consulta" usada em dados XML.

<span id="XML_10QUERY_DESC"></span><span id="xml_10query_desc"></span>**\_10QUERY \_ desc. xml**  
ID de recurso para a cadeia de caracteres "10query desc" usada em dados XML.

<span id="XML_11QUERY_DESC"></span><span id="xml_11query_desc"></span>**\_11QUERY \_ desc. xml**  
ID de recurso para a cadeia de caracteres "11query desc" usada em dados XML.

<span id="XML_FLAGS_NONE"></span><span id="xml_flags_none"></span>**\_sinalizadores XML \_ None**  
ID de recurso para cadeia de caracteres "Flags nenhum" usado em dados XML.

<span id="XML_BUFFERUSAGE"></span><span id="xml_bufferusage"></span>**\_BUFFERUSAGE XML**  
ID do recurso para a cadeia de caracteres "bufferusage" usada em dados XML.

<span id="XML_UNKNOWN"></span><span id="xml_unknown"></span>**XML \_ desconhecido**  
ID de recurso para cadeia de caracteres "desconhecido" usada em dados XML.

<span id="XML_MAXFRAMELATENCY"></span><span id="xml_maxframelatency"></span>**\_MAXFRAMELATENCY XML**  
ID do recurso para a cadeia de caracteres "maxframelatency" usada em dados XML.

<span id="XML_SOURCESIZEWIDTH"></span><span id="xml_sourcesizewidth"></span>**\_SOURCESIZEWIDTH XML**  
ID do recurso para a cadeia de caracteres "sourcesizewidth" usada em dados XML.

<span id="XML_SOURCESIZEHEIGHT"></span><span id="xml_sourcesizeheight"></span>**\_SOURCESIZEHEIGHT XML**  
ID do recurso para a cadeia de caracteres "sourcesizeheight" usada em dados XML.

<span id="XML_MATRIXTRANSFORM"></span><span id="xml_matrixtransform"></span>**\_MATRIXTRANSFORM XML**  
ID do recurso para a cadeia de caracteres "MatrixTransform" usada em dados XML.

<span id="XML_TILED_RESOURCE_INFO"></span><span id="xml_tiled_resource_info"></span>**\_informações de \_ recurso de lado de XML \_**  
ID do recurso para a cadeia de caracteres "informações de recurso lado a lado" usadas em dados XML.

<span id="XML_TILES_RESOURCE_SUBRESOURCE_TILINGS"></span><span id="xml_tiles_resource_subresource_tilings"></span>**subrecursos de recursos de \_ blocos XML \_ \_ \_**  
ID de recurso para a cadeia de caracteres "blocos de subrecurso de recurso lado a lado" usados em dados XML.

<span id="XML_DEVICE_CHILD_INFORMATION"></span><span id="xml_device_child_information"></span>**\_informações de \_ filho do dispositivo XML \_**  
ID de recurso para cadeia de caracteres "informações de dispositivo filho" usadas em dados XML.

<span id="XML_DEVICE"></span><span id="xml_device"></span>**\_dispositivo XML**  
ID de recurso para a cadeia de caracteres "dispositivo" usada em dados XML.

<span id="XML_OTHER"></span><span id="xml_other"></span>**\_outro XML**  
ID de recurso para a cadeia de caracteres "other" usada em dados XML.

<span id="XML_PREDICATE"></span><span id="xml_predicate"></span>**\_predicado XML**  
ID de recurso da cadeia de caracteres "Predicate" usada em dados XML.

<span id="XML_PREDICATE_VALUE"></span><span id="xml_predicate_value"></span>**\_valor de predicado XML \_**  
ID do recurso da cadeia de caracteres "valor do predicado" usado em dados XML.

<span id="XML_DEVICE_CONTEXT_TYPE"></span><span id="xml_device_context_type"></span>**\_tipo de \_ contexto do dispositivo XML \_**  
ID do recurso da cadeia de caracteres "tipo de contexto do dispositivo" usado em dados XML.

<span id="XML_BUFFER_HANDLE"></span><span id="xml_buffer_handle"></span>**\_identificador de buffer XML \_**  
ID de recurso para cadeia de caracteres "identificador de buffer" usado em dados XML.

<span id="XML_DEVICE_12"></span><span id="xml_device_12"></span>**\_Dispositivo XML \_ 12**  
ID de recurso para a cadeia de caracteres "dispositivo 12" usado em dados XML.

<span id="XML_MAX_RECORDING_COMMAND_LISTS"></span><span id="xml_max_recording_command_lists"></span>**\_listas de \_ comandos de gravação máxima \_ de XML \_**  
ID do recurso para a cadeia de caracteres "listas de comandos de gravação máxima" usadas em dados XML.

<span id="XML_DEFAULT_COMMAND_QUEUE"></span><span id="xml_default_command_queue"></span>**\_fila de \_ comandos \_ padrão XML**  
ID de recurso da cadeia de caracteres "fila de comando padrão" usada em dados XML.

<span id="XML_D3D11_DEVICE_INFORMATION"></span><span id="xml_d3d11_device_information"></span>**INFORMAÇÕES DE DISPOSITIVO XML \_ D3D11 \_ \_**  
ID do recurso para a cadeia de caracteres "d3d11 device information" usada em dados XML.

<span id="XML_D3D12_DEVICE_INFORMATION"></span><span id="xml_d3d12_device_information"></span>**INFORMAÇÕES DE DISPOSITIVO XML \_ D3D12 \_ \_**  
ID do recurso para a cadeia de caracteres "d3d12 device information" usada em dados XML.

<span id="XML_CONSERVATIVE_RASTER"></span><span id="xml_conservative_raster"></span>**XML \_ CONSERVATIVE \_ RASTER**  
ID do recurso para cadeia de caracteres "raster conservadora" usada em dados XML.

<span id="XML_INPUT_LAYOUT_12"></span><span id="xml_input_layout_12"></span>**LAYOUT \_ DE ENTRADA XML \_ \_ 12**  
ID do recurso para a cadeia de caracteres "layout de entrada 12" usada em dados XML.

<span id="XML_GRAPHICS_PIPELINE_STATE_12"></span><span id="xml_graphics_pipeline_state_12"></span>**ESTADO \_ \_ \_ 12 DO PIPELINE DE GRÁFICOS \_ XML**  
ID do recurso para a cadeia de caracteres "estado 12 do pipeline de gráficos" usado em dados XML.

<span id="XML_BLEND_STATE_12"></span><span id="xml_blend_state_12"></span>**XML \_ BLEND \_ STATE \_ 12**  
ID do recurso para a cadeia de caracteres "blend state 12" usada em dados XML.

<span id="XML_ROOT_SIGNATURE_12"></span><span id="xml_root_signature_12"></span>**XML \_ ROOT \_ SIGNATURE \_ 12**  
ID do recurso para a cadeia de caracteres "assinatura raiz 12" usada em dados XML.

<span id="XML_STREAM_OUTPUT_12"></span><span id="xml_stream_output_12"></span>**SAÍDA \_ DE FLUXO XML \_ \_ 12**  
ID do recurso para a cadeia de caracteres "saída de fluxo 12" usada em dados XML.

<span id="XML_RASTERIZED_STREAM"></span><span id="xml_rasterized_stream"></span>**FLUXO \_ RASTERIZADO \_ XML**  
ID do recurso para cadeia de caracteres "fluxo rasterizado" usado em dados XML.

<span id="XML_SO_DECLARATION_ENTRY_12"></span><span id="xml_so_declaration_entry_12"></span>**XML \_ SO DECLARATION ENTRY \_ \_ \_ 12**  
ID do recurso para a cadeia de caracteres "so declaration entry 12" usada em dados XML.

<span id="XML_START_COMPONENT"></span><span id="xml_start_component"></span>**COMPONENTE \_ INICIAL \_ XML**  
ID do recurso para a cadeia de caracteres "componente inicial" usado em dados XML.

<span id="XML_COMPONENT_COUNT"></span><span id="xml_component_count"></span>**CONTAGEM DE \_ \_ COMPONENTES XML**  
ID do recurso para a cadeia de caracteres "contagem de componentes" usada em dados XML.

<span id="XML_OUTPUT_SLOT"></span><span id="xml_output_slot"></span>**SLOT DE \_ SAÍDA \_ XML**  
ID do recurso para a cadeia de caracteres "slot de saída" usado em dados XML.

<span id="XML_COMPUTE_PIPELINE_STATE_12"></span><span id="xml_compute_pipeline_state_12"></span>**ESTADO \_ \_ \_ 12 DO PIPELINE DE COMPUTAÇÃO \_ XML**  
ID do recurso para a cadeia de caracteres "estado de pipeline de computação 12" usado em dados XML.

<span id="XML_D3D12_SHADER_BYTECODE"></span><span id="xml_d3d12_shader_bytecode"></span>**XML \_ D3D12 \_ SHADER \_ BYTECODE**  
ID do recurso para a cadeia de caracteres "d3d12 shader bytecode" usada em dados XML.

<span id="XML_D3D12_SHADER_BYTECODE_PTR"></span><span id="xml_d3d12_shader_bytecode_ptr"></span>**XML \_ D3D12 \_ SHADER \_ BYTECODE \_ PTR**  
ID do recurso para a cadeia de caracteres "d3d12 shader bytecode ptr" usada em dados XML.

<span id="XML_D3D12_SHADER_BYTECODE_LENGTH"></span><span id="xml_d3d12_shader_bytecode_length"></span>**COMPRIMENTO DO CÓDIGO \_ DE \_ BYTECODE DO \_ SOMBREADOR XML D3D12 \_**  
ID do recurso para cadeia de caracteres "comprimento do código de byte do sombreador d3d12" usado em dados XML.

<span id="XML_D3D12_VIEW_DIMENSION"></span><span id="xml_d3d12_view_dimension"></span>**DIMENSÃO \_ DE EXIBIÇÃO XML D3D12 \_ \_**  
ID do recurso para a cadeia de caracteres "dimensão de exibição d3d12" usada em dados XML.

<span id="XML_D3D12_NOTHING_TO_DEFINE"></span><span id="xml_d3d12_nothing_to_define"></span>**XML \_ D3D12 \_ NOTHING \_ TO \_ DEFINE**  
ID do recurso para a cadeia de caracteres "d3d12 nothing to define" usada em dados XML.

<span id="XML_OFFSET_IN_BYTES"></span><span id="xml_offset_in_bytes"></span>**DESLOCAMENTO \_ XML \_ EM \_ BYTES**  
ID do recurso para a cadeia de caracteres "deslocamento em bytes" usada em dados XML.

<span id="XML_SIZE_IN_BYTES"></span><span id="xml_size_in_bytes"></span>**TAMANHO \_ XML \_ EM \_ BYTES**  
ID do recurso para a cadeia de caracteres "tamanho em bytes" usada em dados XML.

<span id="XML_SO_CUR_OFFSET"></span><span id="xml_so_cur_offset"></span>**XML \_ SO \_ CUR \_ OFFSET**  
ID do recurso para a cadeia de caracteres "so cur offset" usada em dados XML.

<span id="XML_ROOT_SIGNATURE"></span><span id="xml_root_signature"></span>**ASSINATURA \_ RAIZ \_ XML**  
ID do recurso para a cadeia de caracteres "assinatura raiz" usada em dados XML.

<span id="XML_NUM_PARAMETERS"></span><span id="xml_num_parameters"></span>**PARÂMETROS \_ NUM \_ XML**  
ID do recurso para a cadeia de caracteres "num parameters" usada em dados XML.

<span id="XML_ROOT_PARAMETERS"></span><span id="xml_root_parameters"></span>**PARÂMETROS \_ \_ RAIZ XML**  
ID do recurso para a cadeia de caracteres "parâmetros raiz" usada em dados XML.

<span id="XML_ROOT_PARAMETER"></span><span id="xml_root_parameter"></span>**PARÂMETRO \_ RAIZ \_ XML**  
ID do recurso para a cadeia de caracteres "parâmetro raiz" usada em dados XML.

<span id="XML_ROOT_PARAMETER_TYPE"></span><span id="xml_root_parameter_type"></span>**TIPO \_ DE PARÂMETRO RAIZ \_ \_ XML**  
ID do recurso para a cadeia de caracteres "tipo de parâmetro raiz" usada em dados XML.

<span id="XML_ROOT_PARAMETER_SHADER_VISIBILITY"></span><span id="xml_root_parameter_shader_visibility"></span>**VISIBILIDADE \_ DO \_ \_ SOMBREADOR DE PARÂMETRO RAIZ \_ XML**  
ID do recurso para a cadeia de caracteres "visibilidade do sombreador de parâmetro raiz" usada em dados XML.

<span id="XML_DESCRIPTOR_HANDLE"></span><span id="xml_descriptor_handle"></span>**XML \_ DESCRIPTOR \_ HANDLE**  
ID do recurso para a cadeia de caracteres "descriptor handle" usada em dados XML.

<span id="XML_DESCRIPTOR_HANDLE_PTR"></span><span id="xml_descriptor_handle_ptr"></span>**XML \_ DESCRIPTOR \_ HANDLE \_ PTR**  
ID do recurso para a cadeia de caracteres "descriptor handle ptr" usada em dados XML.

<span id="XML_ROOT_CONSTANTS"></span><span id="xml_root_constants"></span>**CONSTANTES \_ RAIZ \_ XML**  
ID do recurso para cadeia de caracteres "constantes raiz" usadas em dados XML.

<span id="XML_ROOT_DESCRIPTOR"></span><span id="xml_root_descriptor"></span>**DESCRITOR \_ RAIZ \_ XML**  
ID do recurso para a cadeia de caracteres "descritor raiz" usada em dados XML.

<span id="XML_ROOT_DESCRIPTOR_SHADER_REGISTER"></span><span id="xml_root_descriptor_shader_register"></span>**REGISTRO \_ DO \_ SOMBREADOR DE DESCRITOR RAIZ \_ \_ XML**  
ID do recurso para a cadeia de caracteres "registro do sombreador do descritor raiz" usado em dados XML.

<span id="XML_ROOT_DESCRIPTOR_REGISTER_SPACE"></span><span id="xml_root_descriptor_register_space"></span>**ESPAÇO \_ DE REGISTRO DO \_ DESCRITOR RAIZ \_ \_ XML**  
ID do recurso para a cadeia de caracteres "espaço de registro do descritor raiz" usado em dados XML.

<span id="XML_ROOT_DESCRIPTOR_TABLE"></span><span id="xml_root_descriptor_table"></span>**TABELA \_ DO \_ DESCRITOR RAIZ \_ XML**  
ID do recurso para a cadeia de caracteres "tabela de descritor raiz" usada em dados XML.

<span id="XML_ROOT_DESCRIPTOR_NUM_RANGES"></span><span id="xml_root_descriptor_num_ranges"></span>**INTERVALOS NUM \_ \_ DO DESCRITOR RAIZ \_ \_ XML**  
ID do recurso para cadeia de caracteres "intervalos num do descritor raiz" usados em dados XML.

<span id="XML_ROOT_DESCRIPTOR_RANGES"></span><span id="xml_root_descriptor_ranges"></span>**INTERVALOS \_ \_ DE DESCRITOR RAIZ \_ XML**  
ID do recurso para cadeia de caracteres "intervalos de descritores raiz" usados em dados XML.

<span id="XML_ROOT_DESCRIPTOR_RANGE"></span><span id="xml_root_descriptor_range"></span>**INTERVALO \_ DO \_ DESCRITOR RAIZ \_ XML**  
ID do recurso para a cadeia de caracteres "intervalo de descritores raiz" usado em dados XML.

<span id="XML_ROOT_DESCRIPTOR_RANGE_TYPE"></span><span id="xml_root_descriptor_range_type"></span>**TIPO \_ DE INTERVALO DE \_ DESCRITOR RAIZ \_ \_ XML**  
ID do recurso para a cadeia de caracteres "tipo de intervalo do descritor raiz" usado em dados XML.

<span id="XML_ROOT_DESCRIPTOR_RANGE_COUNT"></span><span id="xml_root_descriptor_range_count"></span>**CONTAGEM \_ DE \_ INTERVALOS DO DESCRITOR \_ RAIZ \_ XML**  
ID do recurso para a cadeia de caracteres "contagem de intervalos do descritor raiz" usada em dados XML.

<span id="XML_ROOT_DESCRIPTOR_RANGE_BASE_SHADER_REGISTER"></span><span id="xml_root_descriptor_range_base_shader_register"></span>**REGISTRO DO SOMBREADOR BASE DO \_ RANGE DO \_ \_ \_ \_ DESCRITOR \_ RAIZ XML**  
ID do recurso para cadeia de caracteres "registro de sombreador base do intervalo do descritor raiz" usado em dados XML.

<span id="XML_ROOT_DESCRIPTOR_RANGE_REGISTER_SPACE"></span><span id="xml_root_descriptor_range_register_space"></span>**ESPAÇO \_ DE REGISTRO DE INTERVALO DO \_ \_ DESCRITOR \_ RAIZ \_ XML**  
ID do recurso para cadeia de caracteres "espaço de registro de intervalo do descritor raiz" usado em dados XML.

<span id="XML_ROOT_DESCRIPTOR_RANGE_OFFSET"></span><span id="xml_root_descriptor_range_offset"></span>**DESLOCAMENTO \_ DE INTERVALO DO \_ DESCRITOR RAIZ \_ \_ XML**  
ID do recurso para a cadeia de caracteres "deslocamento de intervalo do descritor raiz" usado em dados XML.

<span id="XML_ROOT_CONSTANTS_SHADER_REGISTER"></span><span id="xml_root_constants_shader_register"></span>**REGISTRO \_ DO \_ SOMBREADOR DE CONSTANTES \_ RAIZ \_ XML**  
ID do recurso para a cadeia de caracteres "registro de sombreador de constantes raiz" usada em dados XML.

<span id="XML_ROOT_CONSTANTS_REGISTER_SPACE"></span><span id="xml_root_constants_register_space"></span>**ESPAÇO \_ DE REGISTRO DE \_ CONSTANTES RAIZ \_ \_ XML**  
ID do recurso para a cadeia de caracteres "espaço de registro de constantes raiz" usada em dados XML.

<span id="XML_ROOT_CONSTANTS_INT32_COUNT"></span><span id="xml_root_constants_int32_count"></span>**CONTAGEM DE CONSTANTES RAIZ XML \_ \_ \_ INT32 \_**  
ID do recurso para a cadeia de caracteres "constantes raiz int32 count" usada em dados XML.

<span id="XML_NOT_SET"></span><span id="xml_not_set"></span>**XML \_ NOT \_ SET**  
ID do recurso para cadeia de caracteres "não definida" usada em dados XML.

<span id="XML_ERROR_RETRIEVING_VALUE"></span><span id="xml_error_retrieving_value"></span>**ERRO XML \_ \_ RECUPERANDO \_ O VALOR**  
ID do recurso para a cadeia de caracteres "erro ao recuperar o valor" usado em dados XML.

<span id="XML_VERTEX_SHADER"></span><span id="xml_vertex_shader"></span>**SOMBREADOR \_ DE \_ VÉRTICE XML**  
ID do recurso para a cadeia de caracteres "sombreador de vértice" usado em dados XML.

<span id="XML_PIXEL_SHADER"></span><span id="xml_pixel_shader"></span>**SOMBREADOR DE PIXEL XML \_ \_**  
ID do recurso para a cadeia de caracteres "sombreador de pixel" usada em dados XML.

<span id="XML_DOMAIN_SHADER"></span><span id="xml_domain_shader"></span>**SOMBREADOR \_ DE DOMÍNIO XML \_**  
ID do recurso para a cadeia de caracteres "sombreador de domínio" usada em dados XML.

<span id="XML_HULL_SHADER"></span><span id="xml_hull_shader"></span>**SOMBREADOR \_ DE CHASSI \_ XML**  
ID do recurso para a cadeia de caracteres "sombreador de chassi" usado em dados XML.

<span id="XML_GEOMETRY_SHADER"></span><span id="xml_geometry_shader"></span>**SOMBREADOR \_ GEOMETRY \_ XML**  
ID do recurso para a cadeia de caracteres "sombreador de geometria" usado em dados XML.

<span id="XML_ROOT_SIGNATURE_OBJECT"></span><span id="xml_root_signature_object"></span>**OBJETO \_ DE ASSINATURA RAIZ \_ \_ XML**  
ID do recurso para a cadeia de caracteres "objeto de assinatura raiz" usado em dados XML.

<span id="XML_ROOT_SIGNATURE_DESERIALIZED_DESC"></span><span id="xml_root_signature_deserialized_desc"></span>**\_DESC \_ \_ DESC DE ASSINATURA RAIZ \_ XML**  
ID do recurso para a cadeia de caracteres "desc desserializada de assinatura raiz" usada em dados XML.

<span id="XML_COMMAND_QUEUE"></span><span id="xml_command_queue"></span>**FILA \_ DE COMANDOS \_ XML**  
ID do recurso para a cadeia de caracteres "fila de comandos" usada em dados XML.

<span id="XML_D3D12_COMMAND_LIST_TYPE_TYPE"></span><span id="xml_d3d12_command_list_type_type"></span>**TIPO DE TIPO DE LISTA DE COMANDOS XML \_ D3D12 \_ \_ \_ \_**  
ID do recurso para a cadeia de caracteres "tipo de tipo de lista de comandos d3d12" usada em dados XML.

<span id="XML_COMMAND_QUEUE_PRIORITY"></span><span id="xml_command_queue_priority"></span>**PRIORIDADE \_ DA FILA DE COMANDOS \_ \_ XML**  
ID do recurso para a cadeia de caracteres "prioridade da fila de comandos" usada em dados XML.

<span id="XML_COMMAND_QUEUE_IS_DEFAULT"></span><span id="xml_command_queue_is_default"></span>**A FILA DE COMANDOS XML \_ \_ É \_ \_ PADRÃO**  
ID do recurso para a cadeia de caracteres "a fila de comandos é padrão" usada em dados XML.

<span id="XML_COMMAND_ALLOCATOR"></span><span id="xml_command_allocator"></span>**ALOCADOR \_ DE \_ COMANDO XML**  
ID do recurso para a cadeia de caracteres "alocador de comandos" usada em dados XML.

<span id="XML_D3D12_RESOURCE"></span><span id="xml_d3d12_resource"></span>**RECURSO \_ XML D3D12 \_**  
ID do recurso para a cadeia de caracteres "recurso d3d12" usada em dados XML.

<span id="XML_D3D12_DESCRIPTOR_HEAP"></span><span id="xml_d3d12_descriptor_heap"></span>**HEAP \_ DO \_ DESCRITOR XML D3D12 \_**  
ID do recurso para a cadeia de caracteres "heap do descritor d3d12" usada em dados XML.

<span id="XML_D3D12_DESCRIPTOR"></span><span id="xml_d3d12_descriptor"></span>**\_DESCRITOR XML D3D12 \_**  
ID do recurso para a cadeia de caracteres "descritor d3d12" usada em dados XML.

<span id="XML_D3D12_RTV"></span><span id="xml_d3d12_rtv"></span>**XML \_ D3D12 \_ RTV**  
ID do recurso para a cadeia de caracteres "d3d12 rtv" usada em dados XML.

<span id="XML_D3D12_VBV"></span><span id="xml_d3d12_vbv"></span>**XML \_ D3D12 \_ VBV**  
ID do recurso para a cadeia de caracteres "d3d12 vbv" usada em dados XML.

<span id="XML_D3D12_IBV"></span><span id="xml_d3d12_ibv"></span>**XML \_ D3D12 \_ IBV**  
ID do recurso para a cadeia de caracteres "d3d12 ibv" usada em dados XML.

<span id="XML_D3D12_CBV"></span><span id="xml_d3d12_cbv"></span>**XML \_ D3D12 \_ CBV**  
ID do recurso para a cadeia de caracteres "d3d12 cbv" usada em dados XML.

<span id="XML_D3D12_SRV"></span><span id="xml_d3d12_srv"></span>**XML \_ D3D12 \_ SRV**  
ID do recurso para a cadeia de caracteres "d3d12 srv" usada em dados XML.

<span id="XML_D3D12_UAV"></span><span id="xml_d3d12_uav"></span>**XML \_ D3D12 \_ UAV**  
ID do recurso para a cadeia de caracteres "d3d12 uav" usada em dados XML.

<span id="XML_D3D12_SOV"></span><span id="xml_d3d12_sov"></span>**XML \_ D3D12 \_ SOV**  
ID do recurso para a cadeia de caracteres "d3d12 sov" usada em dados XML.

<span id="XML_D3D12_SAMPLER"></span><span id="xml_d3d12_sampler"></span>**EXEMPLO DE XML \_ D3D12 \_**  
ID do recurso para a cadeia de caracteres "d3d12 sampler" usada em dados XML.

<span id="XML_D3D12_DSV"></span><span id="xml_d3d12_dsv"></span>**XML \_ D3D12 \_ DSV**  
ID do recurso para a cadeia de caracteres "d3d12 dsv" usada em dados XML.

<span id="XML_TEXTURE_1D"></span><span id="xml_texture_1d"></span>**TEXTURA XML \_ \_ 1D**  
ID do recurso para a cadeia de caracteres "textura 1d" usada em dados XML.

<span id="XML_TEXTURE_1DARRAY"></span><span id="xml_texture_1darray"></span>**TEXTURA XML \_ \_ 1DARRAY**  
ID do recurso para a cadeia de caracteres "textura 1darray" usada em dados XML.

<span id="XML_TEXTURE_2D"></span><span id="xml_texture_2d"></span>**TEXTURA XML \_ \_ 2D**  
ID do recurso para a cadeia de caracteres "textura 2d" usada em dados XML.

<span id="XML_TEXTURE_2DARRAY"></span><span id="xml_texture_2darray"></span>**TEXTURA \_ XML \_ 2DARRAY**  
ID do recurso para a cadeia de caracteres "textura 2darray" usada em dados XML.

<span id="XML_TEXTURE_2DMS"></span><span id="xml_texture_2dms"></span>**TEXTURA XML \_ \_ 2DMS**  
ID do recurso para a cadeia de caracteres "textura 2dms" usada em dados XML.

<span id="XML_TEXTURE_2DMSARRAY"></span><span id="xml_texture_2dmsarray"></span>**TEXTURA \_ XML \_ 2DMSARRAY**  
ID do recurso para a cadeia de caracteres "textura 2dmsarray" usada em dados XML.

<span id="XML_TEXTURE_3D"></span><span id="xml_texture_3d"></span>**TEXTURA XML \_ \_ 3D**  
ID do recurso para a cadeia de caracteres "textura 3d" usada em dados XML.

<span id="XML_MISCELLANEOUS"></span><span id="xml_miscellaneous"></span>**XML \_ DIVERSOS**  
ID do recurso para a cadeia de caracteres "diversos" usada em dados XML.

<span id="XML_RENDER_TARGET_VIEW_12"></span><span id="xml_render_target_view_12"></span>**EXIBIÇÃO DE DESTINO DE RENDERIZAÇÃO XML \_ \_ \_ \_ 12**  
ID do recurso para a cadeia de caracteres "renderizar exibição de destino 12" usada em dados XML.

<span id="XML_STRIDE_COUNT"></span><span id="xml_stride_count"></span>**CONTAGEM \_ DE \_ ESTRIDENTE XML**  
ID do recurso para a "contagem de estridência" de cadeia de caracteres usada em dados XML.

<span id="XML_SHADER_ROOT_SIGNATURES"></span><span id="xml_shader_root_signatures"></span>**ASSINATURAS \_ RAIZ \_ DO SOMBREADOR XML \_**  
ID do recurso para a cadeia de caracteres "assinaturas raiz do sombreador" usada em dados XML.

<span id="XML_CMDLIST_ROOT_SIGNATURE_OBJECT"></span><span id="xml_cmdlist_root_signature_object"></span>**OBJETO \_ DE ASSINATURA RAIZ XML CMDLIST \_ \_ \_**  
ID do recurso para a cadeia de caracteres "objeto de assinatura raiz cmdlist" usado em dados XML.

<span id="XML_INPUT_LAYOUT_ELEMENT_COUNT"></span><span id="xml_input_layout_element_count"></span>**CONTAGEM \_ DE ELEMENTOS DE LAYOUT DE ENTRADA \_ \_ \_ XML**  
ID do recurso para a cadeia de caracteres "contagem de elementos de layout de entrada" usada em dados XML.

<span id="XML_INPUT_LAYOUT_ELEMENT_DESCRIPTIONS"></span><span id="xml_input_layout_element_descriptions"></span>**DESCRIÇÕES \_ DO ELEMENTO DE LAYOUT DE \_ \_ \_ ENTRADA XML**  
ID do recurso para a cadeia de caracteres "descrições do elemento de layout de entrada" usada em dados XML.

<span id="XML_SHADER_DESCRIPTION"></span><span id="xml_shader_description"></span>**DESCRIÇÃO \_ DO SOMBREADOR \_ XML**  
ID do recurso para a cadeia de caracteres "descrição do sombreador" usada em dados XML.

<span id="XML_PSO_ROOT_SIGNATURE_OBJECT"></span><span id="xml_pso_root_signature_object"></span>**OBJETO \_ DE ASSINATURA RAIZ XML PSO \_ \_ \_**  
ID do recurso para a cadeia de caracteres "objeto de assinatura raiz pso" usado em dados XML.

<span id="XML_BUFFER_LOCATION"></span><span id="xml_buffer_location"></span>**LOCAL \_ DO BUFFER \_ XML**  
ID do recurso para a cadeia de caracteres "local do buffer" usada em dados XML.

<span id="XML_BUFFER_FILLED_LOCATION"></span><span id="xml_buffer_filled_location"></span>**LOCAL \_ PREENCHIDO DO BUFFER \_ \_ XML**  
ID do recurso para a cadeia de caracteres "local preenchido pelo buffer" usado em dados XML.

<span id="XML_D3D12_VIRTUAL_ADDRESS"></span><span id="xml_d3d12_virtual_address"></span>**ENDEREÇO \_ VIRTUAL XML D3D12 \_ \_**  
ID do recurso para a cadeia de caracteres "d3d12 endereço virtual" usada em dados XML.

<span id="XML_D3D12_VIRTUAL_ADDRESS_OFFSET"></span><span id="xml_d3d12_virtual_address_offset"></span>**DESLOCAMENTO DE ENDEREÇO VIRTUAL XML \_ D3D12 \_ \_ \_**  
ID do recurso para a cadeia de caracteres "deslocamento de endereço virtual d3d12" usada em dados XML.

<span id="XML_D3D12_OFFSET_IN_BYTES"></span><span id="xml_d3d12_offset_in_bytes"></span>**DESLOCAMENTO \_ XML D3D12 \_ EM \_ \_ BYTES**  
ID do recurso para a cadeia de caracteres "deslocamento d3d12 em bytes" usada em dados XML.

<span id="XML_MAPPED_TILES"></span><span id="xml_mapped_tiles"></span>**BLOCOS \_ MAPEADOS \_ XML**  
ID do recurso para cadeia de caracteres "blocos mapeados" usados em dados XML.

<span id="XML_POOL_TILES"></span><span id="xml_pool_tiles"></span>**BLOCOS \_ DO POOL DE XML \_**  
ID do recurso para cadeia de caracteres "blocos de pool" usados em dados XML.

<span id="XML_TILE"></span><span id="xml_tile"></span>**XML \_ TILE**  
ID do recurso para a cadeia de caracteres "tile" usada em dados XML.

<span id="XML_OPTIONS"></span><span id="xml_options"></span>**OPÇÕES \_ XML**  
ID do recurso para "opções" de cadeia de caracteres usada em dados XML.

<span id="XML_TILE_LIST_MODE"></span><span id="xml_tile_list_mode"></span>**MODO \_ DE LISTA DE \_ TILE \_ XML**  
ID do recurso para a cadeia de caracteres "modo de lista de tiles" usada em dados XML.

<span id="XML_TILE_POOL_SLOT"></span><span id="xml_tile_pool_slot"></span>**SLOT \_ DO POOL DE TILE \_ \_ XML**  
ID do recurso para a cadeia de caracteres "slot do pool de nós" usado em dados XML.

<span id="XML_TILE_COORDINATE"></span><span id="xml_tile_coordinate"></span>**COORDENADA \_ DE TILE \_ XML**  
ID do recurso para a cadeia de caracteres "coordenada de tile" usada em dados XML.

<span id="XML_HEAP_TYPE"></span><span id="xml_heap_type"></span>**\_tipo de heap XML \_**  
ID do recurso para a cadeia de caracteres "tipo de heap" usado em dados XML.

<span id="XML_RESOURCE_USAGE"></span><span id="xml_resource_usage"></span>**\_uso de recursos XML \_**  
ID do recurso para a cadeia de caracteres "uso de recursos" usada em dados XML.

<span id="XML_RESOURCE_USAGES"></span><span id="xml_resource_usages"></span>**\_usos de recursos XML \_**  
ID de recurso para "usos de recursos" da cadeia de caracteres usada em dados XML.

<span id="XML_SUBRESOURCE"></span><span id="xml_subresource"></span>**subrecurso XML \_**  
ID de recurso para a cadeia de caracteres "subresource" usada em dados XML.

<span id="XML_SUBRESOURCE_USAGE"></span><span id="xml_subresource_usage"></span>**\_uso de SUBRECURSO XML \_**  
ID do recurso para a cadeia de caracteres "uso do subrecurso" usado em dados XML.

<span id="XML_COMPUTE_SHADER"></span><span id="xml_compute_shader"></span>**\_sombreador de computação XML \_**  
ID do recurso para a cadeia de caracteres "Compute Shader" usado em dados XML.

<span id="XML_COMMAND_LIST_12"></span><span id="xml_command_list_12"></span>**\_Lista de comandos XML \_ \_ 12**  
ID de recurso da cadeia de caracteres "lista de comandos 12" usada em dados XML.

<span id="XML_PSO_RENDER_TARGET_FORMATS"></span><span id="xml_pso_render_target_formats"></span>**\_formatos de \_ destino de RENDERIZAÇÃO PSO \_ XML \_**  
ID de recurso para cadeia de caracteres "formatos de destino de renderização PSO" usados em dados XML.

<span id="XML_RENDER_TARGET_VIEWS"></span><span id="xml_render_target_views"></span>**\_ \_ exibições de destino de renderização XML \_**  
ID de recurso para cadeia de caracteres "renderizar exibições de destino" usadas em dados XML.

<span id="XML_SHADER_RESOURCE_VIEWS"></span><span id="xml_shader_resource_views"></span>**\_ \_ exibições de recursos do SOMBREAdor XML \_**  
ID do recurso para cadeia de caracteres "exibições de recursos do sombreador" usadas em dados XML.

## <a name="requirements"></a>Requisitos

<table><colgroup><col  /><col  /></colgroup><tbody><tr class="odd"><td><p>Cabeçalho</p></td><td>Vspixengine. h</td></tr></tbody></table>

 

 



