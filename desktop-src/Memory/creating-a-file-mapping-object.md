---
description: A primeira etapa no mapeamento de um arquivo é abrir o arquivo chamando a função CreateFile.
ms.assetid: e00d8742-b717-419c-902c-9a286d75d8aa
title: Criando um objeto de mapeamento de arquivo
ms.topic: article
ms.date: 05/31/2018
ms.openlocfilehash: 550609cf9d8a052e324c585fc046472278bb428c
ms.sourcegitcommit: 8ebcf6cd36f67f8bcf78e76ae8923d65b8995c8a
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 06/05/2021
ms.locfileid: "111524350"
---
# <a name="creating-a-file-mapping-object"></a><span data-ttu-id="00389-103">Criando um objeto de mapeamento de arquivo</span><span class="sxs-lookup"><span data-stu-id="00389-103">Creating a File Mapping Object</span></span>

<span data-ttu-id="00389-104">A primeira etapa no mapeamento de um arquivo é abrir o arquivo chamando a função [**CreateFile**](/windows/win32/api/fileapi/nf-fileapi-createfilea) .</span><span class="sxs-lookup"><span data-stu-id="00389-104">The first step in mapping a file is to open the file by calling the [**CreateFile**](/windows/win32/api/fileapi/nf-fileapi-createfilea) function.</span></span> <span data-ttu-id="00389-105">Para garantir que outros processos não possam gravar na parte do arquivo que está mapeado, você deve abrir o arquivo com acesso exclusivo.</span><span class="sxs-lookup"><span data-stu-id="00389-105">To ensure that other processes cannot write to the portion of the file that is mapped, you should open the file with exclusive access.</span></span> <span data-ttu-id="00389-106">Além disso, o identificador de arquivo deve permanecer aberto até que o processo não precise mais do objeto de mapeamento de arquivo.</span><span class="sxs-lookup"><span data-stu-id="00389-106">In addition, the file handle should remain open until the process no longer needs the file mapping object.</span></span> <span data-ttu-id="00389-107">Uma maneira fácil de obter acesso exclusivo é especificar zero no parâmetro *fdwShareMode* de **CreateFile**.</span><span class="sxs-lookup"><span data-stu-id="00389-107">An easy way to obtain exclusive access is to specify zero in the *fdwShareMode* parameter of **CreateFile**.</span></span> <span data-ttu-id="00389-108">O identificador retornado por **CreateFile** é usado pela função [**CreateFileMapping**](/windows/desktop/api/WinBase/nf-winbase-createfilemappinga) para criar um objeto de mapeamento de arquivo.</span><span class="sxs-lookup"><span data-stu-id="00389-108">The handle returned by **CreateFile** is used by the [**CreateFileMapping**](/windows/desktop/api/WinBase/nf-winbase-createfilemappinga) function to create a file mapping object.</span></span>

<span data-ttu-id="00389-109">A função [**CreateFileMapping**](/windows/desktop/api/WinBase/nf-winbase-createfilemappinga) retorna um identificador para o objeto de mapeamento de arquivo.</span><span class="sxs-lookup"><span data-stu-id="00389-109">The [**CreateFileMapping**](/windows/desktop/api/WinBase/nf-winbase-createfilemappinga) function returns a handle to the file mapping object.</span></span> <span data-ttu-id="00389-110">Esse identificador será usado ao [criar uma exibição de arquivo](creating-a-file-view.md) para que você possa acessar a memória compartilhada.</span><span class="sxs-lookup"><span data-stu-id="00389-110">This handle will be used when [creating a file view](creating-a-file-view.md) so that you can access the shared memory.</span></span> <span data-ttu-id="00389-111">Ao chamar **CreateFileMapping**, você especifica um nome de objeto, o número de bytes a serem mapeados a partir do arquivo e a permissão de leitura/gravação para a memória mapeada.</span><span class="sxs-lookup"><span data-stu-id="00389-111">When you call **CreateFileMapping**, you specify an object name, the number of bytes to be mapped from the file, and the read/write permission for the mapped memory.</span></span> <span data-ttu-id="00389-112">O primeiro processo que chama **CreateFileMapping** cria o objeto de mapeamento de arquivo.</span><span class="sxs-lookup"><span data-stu-id="00389-112">The first process that calls **CreateFileMapping** creates the file mapping object.</span></span> <span data-ttu-id="00389-113">Os processos que chamam **CreateFileMapping** para um objeto existente recebem um identificador para o objeto existente.</span><span class="sxs-lookup"><span data-stu-id="00389-113">Processes calling **CreateFileMapping** for an existing object receive a handle to the existing object.</span></span> <span data-ttu-id="00389-114">Você pode informar se uma chamada bem-sucedida para **CreateFileMapping** criou ou abriu o objeto de mapeamento de arquivo chamando a função [**GetLastError**](/windows/win32/api/errhandlingapi/nf-errhandlingapi-getlasterror) .</span><span class="sxs-lookup"><span data-stu-id="00389-114">You can tell whether or not a successful call to **CreateFileMapping** created or opened the file mapping object by calling the [**GetLastError**](/windows/win32/api/errhandlingapi/nf-errhandlingapi-getlasterror) function.</span></span> <span data-ttu-id="00389-115">**GetLastError** **não retorna nenhum \_ erro** para o processo de criação e o **erro \_ já \_ existe** nos processos subsequentes.</span><span class="sxs-lookup"><span data-stu-id="00389-115">**GetLastError** returns **NO\_ERROR** to the creating process and **ERROR\_ALREADY\_EXISTS** to subsequent processes.</span></span>

<span data-ttu-id="00389-116">A função [**CreateFileMapping**](/windows/desktop/api/WinBase/nf-winbase-createfilemappinga) falhará se os sinalizadores de acesso entrarem em conflito com aqueles especificados quando a função [**CreateFile**](/windows/win32/api/fileapi/nf-fileapi-createfilea) abrir o arquivo.</span><span class="sxs-lookup"><span data-stu-id="00389-116">The [**CreateFileMapping**](/windows/desktop/api/WinBase/nf-winbase-createfilemappinga) function fails if the access flags conflict with those specified when the [**CreateFile**](/windows/win32/api/fileapi/nf-fileapi-createfilea) function opened the file.</span></span> <span data-ttu-id="00389-117">Por exemplo, para ler e gravar no arquivo:</span><span class="sxs-lookup"><span data-stu-id="00389-117">For example, to read and write to the file:</span></span>

-   <span data-ttu-id="00389-118">Especifique os valores de **\_ leitura genérica** e de **\_ gravação genérica** no parâmetro *fdwAccess* de [**CreateFile**](/windows/win32/api/fileapi/nf-fileapi-createfilea).</span><span class="sxs-lookup"><span data-stu-id="00389-118">Specify the **GENERIC\_READ** and **GENERIC\_WRITE** values in the *fdwAccess* parameter of [**CreateFile**](/windows/win32/api/fileapi/nf-fileapi-createfilea).</span></span>
-   <span data-ttu-id="00389-119">Especifique o valor de **\_ ReadWrite da página** no parâmetro *fdwProtect* de [**CreateFileMapping**](/windows/desktop/api/WinBase/nf-winbase-createfilemappinga).</span><span class="sxs-lookup"><span data-stu-id="00389-119">Specify the **PAGE\_READWRITE** value in the *fdwProtect* parameter of [**CreateFileMapping**](/windows/desktop/api/WinBase/nf-winbase-createfilemappinga).</span></span>

<span data-ttu-id="00389-120">A criação de um objeto de mapeamento de arquivo não confirma a memória física; ela só a reserva.</span><span class="sxs-lookup"><span data-stu-id="00389-120">Creating a file mapping object does not commit physical memory, it only reserves it.</span></span>

## <a name="file-mapping-size"></a><span data-ttu-id="00389-121">Tamanho do mapeamento de arquivo</span><span class="sxs-lookup"><span data-stu-id="00389-121">File Mapping Size</span></span>

<span data-ttu-id="00389-122">O tamanho do objeto de mapeamento de arquivo é independente do tamanho do arquivo que está sendo mapeado.</span><span class="sxs-lookup"><span data-stu-id="00389-122">The size of the file mapping object is independent of the size of the file being mapped.</span></span> <span data-ttu-id="00389-123">No entanto, se o objeto de mapeamento de arquivo for maior que o arquivo, o sistema expandirá o arquivo antes que [**CreateFileMapping**](/windows/desktop/api/WinBase/nf-winbase-createfilemappinga) seja retornado.</span><span class="sxs-lookup"><span data-stu-id="00389-123">However, if the file mapping object is larger than the file, the system expands the file before [**CreateFileMapping**](/windows/desktop/api/WinBase/nf-winbase-createfilemappinga) returns.</span></span> <span data-ttu-id="00389-124">Se o objeto de mapeamento de arquivo for menor do que o arquivo, o sistema mapeará apenas o número especificado de bytes do arquivo.</span><span class="sxs-lookup"><span data-stu-id="00389-124">If the file mapping object is smaller than the file, the system maps only the specified number of bytes from the file.</span></span>

<span data-ttu-id="00389-125">Os parâmetros *dwMaximumSizeHigh* e *dwMaximumSizeLow* de [**CreateFileMapping**](/windows/desktop/api/WinBase/nf-winbase-createfilemappinga) permitem que você especifique o número de bytes a serem mapeados a partir do arquivo:</span><span class="sxs-lookup"><span data-stu-id="00389-125">The *dwMaximumSizeHigh* and *dwMaximumSizeLow* parameters of [**CreateFileMapping**](/windows/desktop/api/WinBase/nf-winbase-createfilemappinga) allow you to specify the number of bytes to be mapped from the file:</span></span>

-   <span data-ttu-id="00389-126">Quando você não quiser que o tamanho do arquivo seja alterado (por exemplo, ao mapear arquivos somente leitura), chame [**CreateFileMapping**](/windows/desktop/api/WinBase/nf-winbase-createfilemappinga) e especifique zero para *dwMaximumSizeHigh* e *dwMaximumSizeLow*.</span><span class="sxs-lookup"><span data-stu-id="00389-126">When you do not want the size of the file to change (for example, when mapping read-only files), call [**CreateFileMapping**](/windows/desktop/api/WinBase/nf-winbase-createfilemappinga) and specify zero for both *dwMaximumSizeHigh* and *dwMaximumSizeLow*.</span></span> <span data-ttu-id="00389-127">Isso cria um objeto de mapeamento de arquivo que é exatamente o mesmo tamanho do arquivo.</span><span class="sxs-lookup"><span data-stu-id="00389-127">Doing this creates a file mapping object that is exactly the same size as the file.</span></span> <span data-ttu-id="00389-128">Caso contrário, você deve calcular ou estimar o tamanho do arquivo concluído porque os objetos de mapeamento de arquivo são estáticos em tamanho; uma vez criado, seu tamanho não pode ser aumentado ou diminuído.</span><span class="sxs-lookup"><span data-stu-id="00389-128">Otherwise, you must calculate or estimate the size of the finished file because file mapping objects are static in size; once created, their size cannot be increased or decreased.</span></span> <span data-ttu-id="00389-129">Uma tentativa de mapear um arquivo com um comprimento igual a zero dessa maneira falha com um código de erro de **arquivo de erro \_ \_ inválido**.</span><span class="sxs-lookup"><span data-stu-id="00389-129">An attempt to map a file with a length of zero in this manner fails with an error code of **ERROR\_FILE\_INVALID**.</span></span> <span data-ttu-id="00389-130">Os programas devem testar arquivos com um comprimento de zero e rejeitar esses arquivos.</span><span class="sxs-lookup"><span data-stu-id="00389-130">Programs should test for files with a length of zero and reject such files.</span></span>

-   <span data-ttu-id="00389-131">O tamanho de um objeto de mapeamento de arquivo que é apoiado por um arquivo nomeado é limitado pelo espaço em disco.</span><span class="sxs-lookup"><span data-stu-id="00389-131">The size of a file mapping object that is backed by a named file is limited by disk space.</span></span> <span data-ttu-id="00389-132">O tamanho de uma exibição de arquivo é limitado ao maior bloco contíguo disponível de memória virtual não reservada.</span><span class="sxs-lookup"><span data-stu-id="00389-132">The size of a file view is limited to the largest available contiguous block of unreserved virtual memory.</span></span> <span data-ttu-id="00389-133">Isso tem no máximo 2 GB menos a memória virtual já reservada pelo processo.</span><span class="sxs-lookup"><span data-stu-id="00389-133">This is at most 2 GB minus the virtual memory already reserved by the process.</span></span>

<span data-ttu-id="00389-134">O tamanho do objeto de mapeamento de arquivo que você seleciona controla o quanto no arquivo você pode "ver" com o mapeamento de memória.</span><span class="sxs-lookup"><span data-stu-id="00389-134">The size of the file mapping object that you select controls how far into the file you can "see" with memory mapping.</span></span> <span data-ttu-id="00389-135">Se você criar um objeto de mapeamento de arquivo que tenha 500 KB de tamanho, terá acesso somente ao primeiro 500 KB do arquivo, independentemente do tamanho do arquivo.</span><span class="sxs-lookup"><span data-stu-id="00389-135">If you create a file mapping object that is 500 Kb in size, you have access only to the first 500 Kb of the file, regardless of the size of the file.</span></span> <span data-ttu-id="00389-136">Como não custa a você nenhum recurso do sistema para criar um objeto de mapeamento de arquivo maior, crie um objeto de mapeamento de arquivo que seja o tamanho do arquivo (defina os parâmetros *dwMaximumSizeHigh* e *dwMaximumSizeLow* de [**CreateFileMapping**](/windows/desktop/api/WinBase/nf-winbase-createfilemappinga) como zero), mesmo se você não espera exibir o arquivo inteiro.</span><span class="sxs-lookup"><span data-stu-id="00389-136">Since it does not cost you any system resources to create a larger file mapping object, create a file mapping object that is the size of the file (set the *dwMaximumSizeHigh* and *dwMaximumSizeLow* parameters of [**CreateFileMapping**](/windows/desktop/api/WinBase/nf-winbase-createfilemappinga) both to zero) even if you do not expect to view the entire file.</span></span> <span data-ttu-id="00389-137">O custo nos recursos do sistema é criar os modos de exibição e acessá-los.</span><span class="sxs-lookup"><span data-stu-id="00389-137">The cost in system resources comes in creating the views and accessing them.</span></span>

<span data-ttu-id="00389-138">Você pode exibir uma parte do arquivo que não inicia no início do arquivo.</span><span class="sxs-lookup"><span data-stu-id="00389-138">You can view a portion of the file that does not start at the beginning of the file.</span></span> <span data-ttu-id="00389-139">Para obter mais informações, consulte [criando uma exibição dentro de um arquivo](creating-a-view-within-a-file.md).</span><span class="sxs-lookup"><span data-stu-id="00389-139">For more information, see [Creating a View Within a File](creating-a-view-within-a-file.md).</span></span>

## <a name="related-topics"></a><span data-ttu-id="00389-140">Tópicos relacionados</span><span class="sxs-lookup"><span data-stu-id="00389-140">Related topics</span></span>

<dl> <dt>
  
[<span data-ttu-id="00389-141">Criando uma exibição de arquivo</span><span class="sxs-lookup"><span data-stu-id="00389-141">Creating a File View</span></span>](creating-a-file-view.md)
</dt> <dt>

[<span data-ttu-id="00389-142">Criando uma exibição dentro de um arquivo</span><span class="sxs-lookup"><span data-stu-id="00389-142">Creating a View Within a File</span></span>](creating-a-view-within-a-file.md)
</dt> </dl>


 
