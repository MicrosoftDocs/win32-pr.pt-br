---
description: Sessões de áudio
ms.assetid: b8a1b656-a582-4112-99e9-bd575719ebb3
title: Sessões de áudio
ms.topic: article
ms.date: 05/31/2018
ms.openlocfilehash: 57ae67a3eafe7a76add2fad192823868304e860d
ms.sourcegitcommit: c7add10d695482e1ceb72d62b8a4ebd84ea050f7
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 01/07/2021
ms.locfileid: "104089633"
---
# <a name="audio-sessions"></a><span data-ttu-id="f6005-103">Sessões de áudio</span><span class="sxs-lookup"><span data-stu-id="f6005-103">Audio Sessions</span></span>

<span data-ttu-id="f6005-104">Uma *sessão de áudio* é um grupo de fluxos de áudio relacionados que um cliente WASAPI pode gerenciar coletivamente.</span><span class="sxs-lookup"><span data-stu-id="f6005-104">An *audio session* is a group of related audio streams that a WASAPI client can manage collectively.</span></span> <span data-ttu-id="f6005-105">Os clientes podem controlar o nível de volume e o estado de mudo de cada sessão individual.</span><span class="sxs-lookup"><span data-stu-id="f6005-105">Clients can control the volume level and muting state of each individual session.</span></span> <span data-ttu-id="f6005-106">O sistema aplica o volume especificado pelo cliente e as configurações de mudo uniformemente a todos os fluxos na sessão.</span><span class="sxs-lookup"><span data-stu-id="f6005-106">The system applies client-specified volume and mute settings uniformly to all of the streams in the session.</span></span>

<span data-ttu-id="f6005-107">Quando um cliente Inicializa um fluxo de áudio, ele atribui o fluxo de áudio a uma sessão de áudio.</span><span class="sxs-lookup"><span data-stu-id="f6005-107">When a client initializes an audio stream, it assigns the audio stream to an audio session.</span></span> <span data-ttu-id="f6005-108">Para obter mais informações, consulte [**IAudioClient:: Initialize**](/windows/desktop/api/Audioclient/nf-audioclient-iaudioclient-initialize).</span><span class="sxs-lookup"><span data-stu-id="f6005-108">For more information, see [**IAudioClient::Initialize**](/windows/desktop/api/Audioclient/nf-audioclient-iaudioclient-initialize).</span></span>

<span data-ttu-id="f6005-109">Uma sessão de áudio contém fluxos de renderização ou fluxos de captura, mas não ambos.</span><span class="sxs-lookup"><span data-stu-id="f6005-109">An audio session contains either rendering streams or capture streams, but not both.</span></span> <span data-ttu-id="f6005-110">Por padrão, as configurações de volume e mudo para uma sessão de renderização são persistentes entre as reinicializações do sistema.</span><span class="sxs-lookup"><span data-stu-id="f6005-110">By default, the volume and mute settings for a rendering session are persistent across system restarts.</span></span> <span data-ttu-id="f6005-111">As configurações de volume e mudo para uma sessão de captura não são persistentes.</span><span class="sxs-lookup"><span data-stu-id="f6005-111">The volume and mute settings for a capture session are not persistent.</span></span> <span data-ttu-id="f6005-112">(Uma sessão que contém fluxos que operam no modo de loopback é tratada da mesma forma que uma sessão de captura.</span><span class="sxs-lookup"><span data-stu-id="f6005-112">(A session that contains streams that operate in loopback mode is treated the same as a capture session.</span></span> <span data-ttu-id="f6005-113">Ou seja, as configurações de sessão não são persistentes.</span><span class="sxs-lookup"><span data-stu-id="f6005-113">That is, the session settings are not persistent.</span></span> <span data-ttu-id="f6005-114">Para obter mais informações sobre o modo de loopback, consulte [gravação de loopback](loopback-recording.md).)</span><span class="sxs-lookup"><span data-stu-id="f6005-114">For more information about loopback mode, see [Loopback Recording](loopback-recording.md).)</span></span>

<span data-ttu-id="f6005-115">Cada fluxo de áudio pertence a exatamente uma sessão.</span><span class="sxs-lookup"><span data-stu-id="f6005-115">Every audio stream belongs to exactly one session.</span></span> <span data-ttu-id="f6005-116">Um cliente atribui um fluxo de áudio a uma sessão específica no momento em que ele inicializa o objeto de fluxo.</span><span class="sxs-lookup"><span data-stu-id="f6005-116">A client assigns an audio stream to a particular session at the time that it initializes the stream object.</span></span> <span data-ttu-id="f6005-117">O fluxo retém sua associação na sessão durante o tempo de vida do fluxo.</span><span class="sxs-lookup"><span data-stu-id="f6005-117">The stream retains its membership in the session for the lifetime of the stream.</span></span> <span data-ttu-id="f6005-118">Depois que um objeto de fluxo é criado, o objeto existe até que um cliente libere a última referência contada para o objeto e, em seguida, o objeto seja excluído.</span><span class="sxs-lookup"><span data-stu-id="f6005-118">After a stream object is created, the object exists until a client releases the last counted reference to the object, and then the object is deleted.</span></span>

<span data-ttu-id="f6005-119">Embora um cliente não possa alterar a sessão à qual um fluxo existente é atribuído, ele pode obter um efeito semelhante ao excluir o fluxo (liberando todas as referências a ele), criando um novo fluxo para substituir o fluxo excluído e atribuir o novo fluxo a outra sessão.</span><span class="sxs-lookup"><span data-stu-id="f6005-119">Although a client cannot change the session to which an existing stream is assigned, it can achieve a similar effect by deleting the stream (by releasing all references to it), creating a new stream to replace the deleted stream, and assigning the new stream to another session.</span></span>

<span data-ttu-id="f6005-120">Cada sessão de renderização representa um subconjunto dos fluxos que formam a combinação global que é reproduzida por meio de um [dispositivo de ponto de extremidade de áudio](audio-endpoint-devices.md)específico.</span><span class="sxs-lookup"><span data-stu-id="f6005-120">Each rendering session represents a subset of the streams that form the global mix that plays through a particular [audio endpoint device](audio-endpoint-devices.md).</span></span> <span data-ttu-id="f6005-121">A combinação global combina todas as sessões de todos os aplicativos que compartilham o dispositivo.</span><span class="sxs-lookup"><span data-stu-id="f6005-121">The global mix combines all of the sessions from all of the applications that share the device.</span></span>

<span data-ttu-id="f6005-122">Frequentemente, um aplicativo com vários fluxos atribui todos os seus fluxos à mesma sessão.</span><span class="sxs-lookup"><span data-stu-id="f6005-122">Frequently, an application with several streams assigns all of its streams to the same session.</span></span> <span data-ttu-id="f6005-123">No entanto, o aplicativo pode, como uma opção, atribuir fluxos diferentes a sessões diferentes.</span><span class="sxs-lookup"><span data-stu-id="f6005-123">However, the application can, as an option, assign different streams to different sessions.</span></span> <span data-ttu-id="f6005-124">Qualquer fluxo que o aplicativo não atribui explicitamente a uma sessão pertence à sessão padrão.</span><span class="sxs-lookup"><span data-stu-id="f6005-124">Any stream that the application does not explicitly assign to a session belongs to the default session.</span></span>

<span data-ttu-id="f6005-125">Os aplicativos de áudio típicos devem evitar modificar as configurações de volume e mudo para as sessões.</span><span class="sxs-lookup"><span data-stu-id="f6005-125">Typical audio applications should avoid modifying the volume and mute settings for sessions.</span></span> <span data-ttu-id="f6005-126">Em vez disso, os usuários controlam essas configurações por meio das interfaces de usuário dos programas de controle.</span><span class="sxs-lookup"><span data-stu-id="f6005-126">Instead, users control these settings through the user interfaces of control programs.</span></span> <span data-ttu-id="f6005-127">Por exemplo, no Windows Vista, o programa fornecido pelo sistema, Sndvol.exe, exibe um controle de volume e controle de mudo para cada sessão de renderização ativa ou recentemente ativa no sistema.</span><span class="sxs-lookup"><span data-stu-id="f6005-127">For example, in Windows Vista, the system-supplied program, Sndvol.exe, displays a volume control and mute control for each active or recently active rendering session in the system.</span></span> <span data-ttu-id="f6005-128">Por meio desses controles, os usuários podem ajustar as configurações de volume e mudo para todas as sessões no sistema.</span><span class="sxs-lookup"><span data-stu-id="f6005-128">Through these controls, users can adjust the volume and mute settings for all of the sessions in the system.</span></span>

<span data-ttu-id="f6005-129">Atualmente, o programa SNDVOL exibe controles de volume somente para dispositivos de ponto de extremidade de renderização de áudio.</span><span class="sxs-lookup"><span data-stu-id="f6005-129">The Sndvol program currently displays volume controls for audio-rendering endpoint devices only.</span></span> <span data-ttu-id="f6005-130">Ele não exibe controles de volume para dispositivos de captura de áudio.</span><span class="sxs-lookup"><span data-stu-id="f6005-130">It does not display volume controls for audio-capture devices.</span></span>

<span data-ttu-id="f6005-131">Uma sessão estará ativa se contiver um ou mais fluxos ativos.</span><span class="sxs-lookup"><span data-stu-id="f6005-131">A session is active if it contains one or more active streams.</span></span> <span data-ttu-id="f6005-132">Um fluxo ativo está no *estado executando*.</span><span class="sxs-lookup"><span data-stu-id="f6005-132">An active stream is in the *running state*.</span></span> <span data-ttu-id="f6005-133">Um fluxo inativo está no *estado parado*.</span><span class="sxs-lookup"><span data-stu-id="f6005-133">An inactive stream is in the *stopped state*.</span></span> <span data-ttu-id="f6005-134">Uma sessão fica ativa quando seu primeiro fluxo se torna ativo.</span><span class="sxs-lookup"><span data-stu-id="f6005-134">A session becomes active when its first stream becomes active.</span></span> <span data-ttu-id="f6005-135">Uma sessão se torna inativa quando seu último fluxo ativo se torna inativo.</span><span class="sxs-lookup"><span data-stu-id="f6005-135">A session becomes inactive when its last active stream becomes inactive.</span></span> <span data-ttu-id="f6005-136">Depois que uma sessão esteve inativa por um período de tempo, o sistema altera o estado da sessão de inativo para expirado.</span><span class="sxs-lookup"><span data-stu-id="f6005-136">After a session has been inactive for a period of time, the system changes the state of the session from inactive to expired.</span></span>

<span data-ttu-id="f6005-137">SNDVOL exibe os controles de volume e sem áudio para todas as sessões de renderização ativas e inativas.</span><span class="sxs-lookup"><span data-stu-id="f6005-137">Sndvol displays volume and mute controls for all active and inactive rendering sessions.</span></span> <span data-ttu-id="f6005-138">SNDVOL remove os controles de volume e de mudo de uma sessão quando o estado da sessão é alterado de inativo para expirado ou quando a sessão é encerrada.</span><span class="sxs-lookup"><span data-stu-id="f6005-138">Sndvol removes the volume and mute controls for a session when the state of the session changes from inactive to expired, or when the session terminates.</span></span> <span data-ttu-id="f6005-139">(Uma sessão é encerrada quando o último de seus fluxos é excluído; ou seja, quando um cliente libera a contagem de referência final no último objeto de fluxo restante na sessão.) A única exceção a essa regra é para sons de notificação do sistema.</span><span class="sxs-lookup"><span data-stu-id="f6005-139">(A session terminates when the last of its streams is deleted; that is, when a client releases the final reference count on the last remaining stream object in the session.) The one exception to this rule is for system-notification sounds.</span></span> <span data-ttu-id="f6005-140">O SNDVOL sempre exibe os controles de volume e mudo para sons de notificação do sistema, independentemente do estado da sessão para esses sons.</span><span class="sxs-lookup"><span data-stu-id="f6005-140">Sndvol always displays the volume and mute controls for system-notification sounds regardless of the state of the session for these sounds.</span></span>

<span data-ttu-id="f6005-141">Normalmente, um fluxo pertence a uma sessão que abrange apenas o processo que contém o aplicativo que criou o fluxo.</span><span class="sxs-lookup"><span data-stu-id="f6005-141">Typically, a stream belongs to a session that spans only the process that contains the application that created the stream.</span></span> <span data-ttu-id="f6005-142">No entanto, os aplicativos têm a opção de definir sessões entre processos que combinam fluxos de dois ou mais processos.</span><span class="sxs-lookup"><span data-stu-id="f6005-142">However, applications have the option of defining cross-process sessions that combine streams from two or more processes.</span></span>

<span data-ttu-id="f6005-143">O WASAPI dá suporte a sessões entre processos principalmente para que:</span><span class="sxs-lookup"><span data-stu-id="f6005-143">WASAPI supports cross-process sessions primarily so that:</span></span>

-   <span data-ttu-id="f6005-144">O programa SNDVOL pode apresentar ao usuário um único controle de volume para gerenciar sons de notificação do sistema em todos os aplicativos.</span><span class="sxs-lookup"><span data-stu-id="f6005-144">The Sndvol program can present the user with a single volume control to manage system-notification sounds across all applications.</span></span>
-   <span data-ttu-id="f6005-145">Um player de mídia executado em um processo pode transmitir conteúdo protegido para um programa de descriptografia que é executado em outro processo.</span><span class="sxs-lookup"><span data-stu-id="f6005-145">A media player that runs in one process can stream protected content to a decryption program that runs in another process.</span></span>

<span data-ttu-id="f6005-146">Semelhante às configurações de controle para sessões de renderização específicas do processo, as configurações de controle para sessões de processamento entre processos são, por padrão, persistentes entre as reinicializações do sistema.</span><span class="sxs-lookup"><span data-stu-id="f6005-146">Similar to the control settings for process-specific rendering sessions, the control settings for cross-process rendering sessions are, by default, persistent across system restarts.</span></span> <span data-ttu-id="f6005-147">O WASAPI fornece esse comportamento principalmente para o benefício de sons de notificação do sistema, que deve manter as configurações de volume e mudo do usuário à medida que a combinação de aplicativos varia ao longo do tempo.</span><span class="sxs-lookup"><span data-stu-id="f6005-147">WASAPI provides this behavior primarily for the benefit of system-notification sounds, which must retain the user's volume and mute settings as the application mix varies over time.</span></span>

<span data-ttu-id="f6005-148">O programa SNDVOL rotula o controle de volume para cada sessão com um nome de exibição e um ícone.</span><span class="sxs-lookup"><span data-stu-id="f6005-148">The Sndvol program labels the volume control for each session with a display name and an icon.</span></span> <span data-ttu-id="f6005-149">Um cliente tem a opção de atribuir explicitamente um nome de exibição e um ícone a uma sessão.</span><span class="sxs-lookup"><span data-stu-id="f6005-149">A client has the option of explicitly assigning a display name and icon to a session.</span></span> <span data-ttu-id="f6005-150">Se o cliente não fornecer esses itens, SNDVOL exibirá um nome padrão e o ícone padrão em vez disso.</span><span class="sxs-lookup"><span data-stu-id="f6005-150">If the client does not provide these items, then Sndvol displays a default name and default icon instead.</span></span> <span data-ttu-id="f6005-151">O nome padrão incorpora informações como o título da janela do aplicativo.</span><span class="sxs-lookup"><span data-stu-id="f6005-151">The default name incorporates information such as the title of the application window.</span></span> <span data-ttu-id="f6005-152">O ícone padrão é o ícone para a janela do aplicativo.</span><span class="sxs-lookup"><span data-stu-id="f6005-152">The default icon is the icon for the application window.</span></span> <span data-ttu-id="f6005-153">Somente no caso de sessões específicas do processo, esses padrões fornecem informações significativas para os usuários.</span><span class="sxs-lookup"><span data-stu-id="f6005-153">Only in the case of process-specific sessions do these defaults provide meaningful information to users.</span></span> <span data-ttu-id="f6005-154">Observe que uma sessão entre processos pode ser associada a mais de um aplicativo.</span><span class="sxs-lookup"><span data-stu-id="f6005-154">Note that a cross-process session can be associated with more than one application.</span></span> <span data-ttu-id="f6005-155">Nesse caso, apenas um nome de exibição e ícone fornecidos pelo cliente são significativos.</span><span class="sxs-lookup"><span data-stu-id="f6005-155">In this case, only a client-provided display name and icon are meaningful.</span></span>

<span data-ttu-id="f6005-156">Embora as configurações de volume e mudo para uma sessão de renderização sejam, por padrão, persistentes entre as reinicializações do sistema, o nome de exibição e o ícone fornecidos pelo cliente não são.</span><span class="sxs-lookup"><span data-stu-id="f6005-156">Although the volume and mute settings for a rendering session are, by default, persistent across system restarts, the client-provided display name and icon are not.</span></span> <span data-ttu-id="f6005-157">Para garantir que o SNDVOL exiba o nome e o ícone fornecidos pelo cliente, o cliente deve atribuir explicitamente o nome e o ícone à sessão no momento em que o cliente atribui o primeiro fluxo à sessão.</span><span class="sxs-lookup"><span data-stu-id="f6005-157">To ensure that Sndvol displays the client-provided name and icon, the client must explicitly assign the name and icon to the session at the time that the client assigns the first stream to the session.</span></span> <span data-ttu-id="f6005-158">O sistema retém o nome de exibição e o ícone para uma sessão somente até que a sessão seja encerrada.</span><span class="sxs-lookup"><span data-stu-id="f6005-158">The system retains the display name and icon for a session only until the session terminates.</span></span>

<span data-ttu-id="f6005-159">Cada sessão é identificada por um GUID de sessão.</span><span class="sxs-lookup"><span data-stu-id="f6005-159">Each session is identified by a session GUID.</span></span> <span data-ttu-id="f6005-160">No momento em que um cliente abre um fluxo, o cliente atribui esse fluxo a uma sessão específica.</span><span class="sxs-lookup"><span data-stu-id="f6005-160">At the time that a client opens a stream, the client assigns that stream to a particular session.</span></span> <span data-ttu-id="f6005-161">O cliente fornece as duas informações a seguir para identificar essa sessão:</span><span class="sxs-lookup"><span data-stu-id="f6005-161">The client supplies the following two pieces of information to identify that session:</span></span>

-   <span data-ttu-id="f6005-162">Um GUID de sessão.</span><span class="sxs-lookup"><span data-stu-id="f6005-162">A session GUID.</span></span>
-   <span data-ttu-id="f6005-163">Se a sessão é uma sessão de processo cruzado ou específico do processo — uma sessão específica do processo contém apenas fluxos do processo do cliente.</span><span class="sxs-lookup"><span data-stu-id="f6005-163">Whether the session is a cross-process or process-specific session—a process-specific session contains only streams from the client's process.</span></span>

<span data-ttu-id="f6005-164">Essas informações são suficientes para distinguir uma sessão específica de todas as outras sessões no mesmo computador.</span><span class="sxs-lookup"><span data-stu-id="f6005-164">This information is sufficient to distinguish a particular session from all other sessions in the same computer.</span></span> <span data-ttu-id="f6005-165">O GUID de sessão para uma sessão específica de processo identifica exclusivamente a sessão somente dentro do escopo do processo que possui a sessão.</span><span class="sxs-lookup"><span data-stu-id="f6005-165">The session GUID for a process-specific session uniquely identifies the session only within the scope of the process that owns the session.</span></span> <span data-ttu-id="f6005-166">Por outro lado, o GUID de sessão para uma sessão de processo cruzado é exclusivo dentro do escopo de todos os processos em execução no computador.</span><span class="sxs-lookup"><span data-stu-id="f6005-166">In contrast, the session GUID for a cross-process session is unique within the scope of all of the processes that are running on the computer.</span></span>

<span data-ttu-id="f6005-167">No caso de uma sessão específica de processo, o sistema usa uma combinação de GUID de sessão e ID de processo para identificar exclusivamente a sessão dentro do escopo do computador.</span><span class="sxs-lookup"><span data-stu-id="f6005-167">In the case of a process-specific session, the system uses a combination of session GUID and process ID to uniquely identify the session within the scope of the computer.</span></span> <span data-ttu-id="f6005-168">Portanto, se os clientes em dois processos diferentes atribuirem seus respectivos fluxos a duas sessões específicas do processo com GUIDs de sessão idênticos, o sistema tratará as sessões como separadas, pois suas IDs de processo são diferentes.</span><span class="sxs-lookup"><span data-stu-id="f6005-168">Thus, if clients in two different processes assign their respective streams to two process-specific sessions with identical session GUIDs, the system treats the sessions as separate because their process IDs are different.</span></span> <span data-ttu-id="f6005-169">Além disso, se uma sessão entre processos usar a mesma GUID de sessão como uma ou mais sessões específicas do processo, o sistema tratará a sessão entre processos como distinta das sessões específicas do processo, mesmo que compartilhem a mesma GUID de sessão.</span><span class="sxs-lookup"><span data-stu-id="f6005-169">In addition, if a cross-process session uses the same session GUID as one or more process-specific sessions, the system treats the cross-process session as distinct from the process-specific sessions, even though they share the same session GUID.</span></span>

<span data-ttu-id="f6005-170">Por exemplo, no Windows Vista, as APIs de nível superior, como as funções **waveOutXxx** de multimídia do Windows e o DirectSound, normalmente atribuem os fluxos de áudio que eles criam para as sessões padrão, específicas do processo, identificadas pelo valor GUID de sessão GUID \_ NULL.</span><span class="sxs-lookup"><span data-stu-id="f6005-170">For example, in Windows Vista, higher-level APIs such as the Windows multimedia **waveOutXxx** functions and DirectSound typically assign the audio streams that they create to default, process-specific sessions that are identified by the session GUID value GUID\_NULL.</span></span> <span data-ttu-id="f6005-171">Para clientes dessas APIs, a sessão padrão para cada processo de cliente é separada das sessões padrão para outros processos de cliente, mesmo que as sessões tenham GUIDs de sessão idênticas.</span><span class="sxs-lookup"><span data-stu-id="f6005-171">For clients of these APIs, the default session for each client process is separate from the default sessions for other client processes, even though the sessions have identical session GUIDs.</span></span> <span data-ttu-id="f6005-172">Além disso, se um ou mais aplicativos atribuirem fluxos à sessão entre processos que é identificada pelo valor GUID de sessão GUID \_ NULL, o sistema tratará essa sessão entre processos como separada das sessões padrão, específicas do processo que compartilham a mesma GUID de sessão.</span><span class="sxs-lookup"><span data-stu-id="f6005-172">In addition, if one or more applications assign streams to the cross-process session that is identified by the session GUID value GUID\_NULL, then the system treats this cross-process session as separate from the default, process-specific sessions that share the same session GUID.</span></span> <span data-ttu-id="f6005-173">Da mesma forma, o programa SNDVOL exibe um controle de volume separado para cada sessão padrão, específica de processo do cliente, e ele exibe um controle de volume adicional para a sessão entre processos que é identificada pelo valor GUID de sessão GUID \_ NULL, caso essa sessão exista.</span><span class="sxs-lookup"><span data-stu-id="f6005-173">Accordingly, the Sndvol program displays a separate volume control for each client's default, process-specific session, and it displays an additional volume control for the cross-process session that is identified by the session GUID value GUID\_NULL, if that session exists.</span></span>

<span data-ttu-id="f6005-174">Cada sessão é associada a apenas um dispositivo de ponto de extremidade de áudio.</span><span class="sxs-lookup"><span data-stu-id="f6005-174">Each session is associated with only one audio endpoint device.</span></span> <span data-ttu-id="f6005-175">Se duas sessões tiverem GUIDs de sessão e IDs de processo idênticos, mas estiverem associadas a dispositivos diferentes, o sistema tratará as duas sessões como separadas.</span><span class="sxs-lookup"><span data-stu-id="f6005-175">If two sessions have identical session GUIDs and process IDs but are associated with different devices, then the system treats the two sessions as separate.</span></span> <span data-ttu-id="f6005-176">Uma sessão nunca pode conter fluxos de captura e de renderização porque um fluxo de captura pode ser associado somente a um dispositivo de captura e um fluxo de renderização só pode ser associado a um dispositivo de renderização.</span><span class="sxs-lookup"><span data-stu-id="f6005-176">A session can never contain both capture and rendering streams because a capture stream can be associated only with a capture device and a rendering stream can be associated only with a rendering device.</span></span>

<span data-ttu-id="f6005-177">Conforme mencionado anteriormente, as configurações de volume e mudo para uma sessão são persistentes entre as reinicializações do sistema.</span><span class="sxs-lookup"><span data-stu-id="f6005-177">As mentioned previously, the volume and mute settings for a session are persistent across system restarts.</span></span> <span data-ttu-id="f6005-178">Duas ou mais instâncias de um aplicativo podem criar sessões específicas do processo com GUIDs de sessão idênticos.</span><span class="sxs-lookup"><span data-stu-id="f6005-178">Two or more instances of an application can create process-specific sessions with identical session GUIDs.</span></span> <span data-ttu-id="f6005-179">Por meio do programa SNDVOL, o usuário pode selecionar configurações diferentes de volume e mudo para cada uma dessas sessões.</span><span class="sxs-lookup"><span data-stu-id="f6005-179">Through the Sndvol program, the user can select different volume and mute settings for each of these sessions.</span></span> <span data-ttu-id="f6005-180">Depois que essas sessões forem encerradas, o sistema manterá as configurações de controle de apenas uma dessas sessões — a última sessão a ser encerrada.</span><span class="sxs-lookup"><span data-stu-id="f6005-180">After these sessions terminate, the system retains the control settings of only one of these sessions—the last session to terminate.</span></span> <span data-ttu-id="f6005-181">Posteriormente, se uma nova instância do aplicativo criar uma sessão específica do processo com o mesmo GUID de sessão anterior, essa sessão herdará as configurações de volume e de mudo salvas anteriormente.</span><span class="sxs-lookup"><span data-stu-id="f6005-181">Later, if a new instance of the application creates a process-specific session with the same session GUID as before, that session inherits the previously saved volume and mute settings.</span></span>

<span data-ttu-id="f6005-182">Um aplicativo não deve tentar adicionar um fluxo ou controlar o nível de volume de uma sessão que pertence a outro aplicativo não relacionado.</span><span class="sxs-lookup"><span data-stu-id="f6005-182">An application should not attempt to add a stream to or control the volume level of a session that is owned by another, unrelated application.</span></span> <span data-ttu-id="f6005-183">Além disso, um aplicativo não deve tentar controlar o nível de volume da sessão gerenciada pelo sistema para sons de notificação.</span><span class="sxs-lookup"><span data-stu-id="f6005-183">In addition, an application should not attempt to control the volume level of the system-managed session for notification sounds.</span></span> <span data-ttu-id="f6005-184">No entanto, um aplicativo pode reproduzir um som por meio da sessão do sistema para sons de notificação chamando a função **PlaySound** .</span><span class="sxs-lookup"><span data-stu-id="f6005-184">However, an application can play a sound through the system session for notification sounds by calling the **PlaySound** function.</span></span> <span data-ttu-id="f6005-185">Para obter mais informações, consulte [sons de notificação para aplicativos de áudio herdados](notification-sounds-for-legacy-audio-applications.md).</span><span class="sxs-lookup"><span data-stu-id="f6005-185">For more information, see [Notification Sounds for Legacy Audio Applications](notification-sounds-for-legacy-audio-applications.md).</span></span>

<span data-ttu-id="f6005-186">Um aplicativo pode se registrar para receber notificações quando o estado de uma sessão é alterado.</span><span class="sxs-lookup"><span data-stu-id="f6005-186">An application can register to receive notifications when the state of a session changes.</span></span> <span data-ttu-id="f6005-187">Para obter mais informações, consulte [eventos de sessão de áudio](audio-session-events.md).</span><span class="sxs-lookup"><span data-stu-id="f6005-187">For more information, see [Audio Session Events](audio-session-events.md).</span></span>

<span data-ttu-id="f6005-188">Em casos raros, um aplicativo que cria uma sessão específica de processo pode precisar consolidar o controle das sessões específicas do processo para duas ou mais instâncias de aplicativo em um único controle de volume no SNDVOL.</span><span class="sxs-lookup"><span data-stu-id="f6005-188">In rare cases, an application that creates a process-specific session might need to consolidate the control of the process-specific sessions for two or more application instances under a single volume control in Sndvol.</span></span> <span data-ttu-id="f6005-189">Para obter mais informações, consulte [agrupando parâmetros](grouping-parameters.md).</span><span class="sxs-lookup"><span data-stu-id="f6005-189">For more information, see [Grouping Parameters](grouping-parameters.md).</span></span>

## <a name="related-topics"></a><span data-ttu-id="f6005-190">Tópicos relacionados</span><span class="sxs-lookup"><span data-stu-id="f6005-190">Related topics</span></span>

<dl> <dt>

[<span data-ttu-id="f6005-191">Guia de programação</span><span class="sxs-lookup"><span data-stu-id="f6005-191">Programming Guide</span></span>](programming-guide.md)
</dt> </dl>

 

 



